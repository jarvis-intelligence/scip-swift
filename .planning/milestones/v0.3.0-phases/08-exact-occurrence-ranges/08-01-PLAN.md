---
phase: 08-exact-occurrence-ranges
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - Package.swift
  - Package.resolved
  - Sources/scip-swift/SourceRefinement/SwiftSyntaxRefiner.swift
  - Sources/scip-swift/SCIPMapping/PositionMapping.swift
  - Sources/scip-swift/SCIPMapping/SCIPIndexBuilder.swift
  - Tests/scip-swiftTests/SwiftSyntaxRefinerTests.swift
  - Tests/scip-swiftTests/PositionMappingTests.swift
  - Tests/scip-swiftTests/IntegrationTests.swift
  - README.md
autonomous: true
requirements: [RANGE-01, RANGE-02, RANGE-03]
estimate:
  tokens: 30000
  raw_tokens: 30000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - The MiniSwiftPackage fixture .scip emits exact identifier-token ends for definitions AND references — a getter:name occurrence whose display-name approximation would drift +7 bytes instead carries the exact [start,end) columns (RANGE-01, research F3a)
    - Occurrence columns after multi-byte content (名前, 日本語, emoji) are UTF-8 byte offsets — the refiner's Unicode corpus rows produce the exact byte-column expectations of research F4, not UTF-16 columns (RANGE-02)
    - A file with syntax errors still yields a usable token map for its valid regions, and an anchor that misses the map falls back to the existing approximation — no throw, no dropped occurrence (RANGE-03, research F5)
    - trailing-trivia regression: the refiner's end is positionAfterSkippingLeadingTrivia.utf8Offset + text.utf8.count, never endPosition (research F1/Pitfall 1 — the `emoji` token ends at 9, not 10)
    - swift-syntax 602.0.0 resolves pinned exact in Package.resolved and the package still builds against the 6.2.4 toolchain (research D1)
  artifacts:
    - path: "Package.swift"
      provides: "swift-syntax 602.0.0 exact-pin dependency with SwiftSyntax + SwiftParser products on both the executable and test targets"
    - path: "Sources/scip-swift/SourceRefinement/SwiftSyntaxRefiner.swift"
      provides: "struct SwiftSyntaxRefiner — failable init?(filePath:) one-parse-per-file, tokenEndColumns map keyed line → 0-based byte startCol → 0-based byte endCol, exactEndColumn(line:utf8Column:) taking 1-based IndexStoreDB values; nil on read failure or map miss; extensible for Phase 9 docs (same parse) with no docs extraction yet (research D2/Pitfall 4)"
    - path: "Sources/scip-swift/SCIPMapping/PositionMapping.swift"
      provides: "singleLineRange(location:displayName:exactEndColumn: Int32?) — identical anchor math, endCharacter from exact end when non-nil else the existing approximateTokenLength path unchanged (research D3)"
    - path: "Sources/scip-swift/SCIPMapping/SCIPIndexBuilder.swift"
      provides: "makeDocument constructs SwiftSyntaxRefiner(filePath:) once per non-cached document and passes the lookup result into singleLineRange; nil refiner → nil exactEndColumn → approximation"
    - path: "Tests/scip-swiftTests/SwiftSyntaxRefinerTests.swift"
      provides: "unit corpus: line-table math, exact-extent invariants, Unicode byte-column expectations (F4), trailing-trivia regression, error-node tokens, anchor-miss → nil, unreadable path → nil init"
    - path: "Tests/scip-swiftTests/PositionMappingTests.swift"
      provides: "pure-unit fallback-contract suite: exact end used when present, approximation byte-identical to today when nil, 1-based→0-based math"
  key_links:
    - Package.swift exact pin → Package.resolved 602.0.0 → test target compiles with import SwiftSyntax/SwiftParser
    - SwiftSyntaxRefiner.exactEndColumn(line:utf8Column:) → PositionMapping.singleLineRange(exactEndColumn:) → SCIPIndexBuilder.makeDocument occurrence loop
    - makeDocument cache-hit branch (documents served from CacheStore) → already-exact cached ranges, no re-parse (research D2 — cache hits skip makeDocument entirely)
---

<objective>
Add the swift-syntax 602.0.0 exact-pin dependency (research D1; binary 7→24.5 MB, cold build 26→261s — trade-off accepted per research open-questions sign-off) and build SwiftSyntaxRefiner as a pure module with its full unit corpus, then wire ONE fixture file end-to-end as the tracer: MiniSwiftPackage's occurrences emit exact token ends in a real .scip, compound-name/accessor drift included (RANGE-01), Unicode rows proven at unit level (RANGE-02), fallback contract proven at unit level (RANGE-03).

Purpose: Every occurrence's end column today is guessed from display-name length; the guess measurably drifts on accessor and interpolation anchors, and Phase 9 needs the same single parse for docs.
Output: Dependency + SwiftSyntaxRefiner module + PositionMapping exact-or-approximate signature + tracer builder wiring + two new unit suites.
</objective>

<execution_context>
@$HOME/.codex/gsd-core/workflows/execute-plan.md
@$HOME/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-exact-occurrence-ranges/08-RESEARCH.md
@.planning/phases/08-exact-occurrence-ranges/08-VALIDATION.md

@Package.swift
@Sources/scip-swift/SCIPMapping/PositionMapping.swift
@Sources/scip-swift/SCIPMapping/SCIPIndexBuilder.swift
@Tests/scip-swiftTests/IntegrationTests.swift
@Tests/scip-swiftTests/SymbolKindMappingTests.swift
</context>

<tasks>

<task type="auto">
  <name>Task 1: swift-syntax 602.0.0 exact pin + resolve</name>
  <files>Package.swift, Package.resolved, README.md</files>
  <precondition>The .swift-version toolchain (6.2.4) is active and network is available for the one-time resolve — `swift package resolve` must pin 602.0.0, not a newer 603/604/605 (research D1). Expect the first `swift build` after this task to take several minutes cold; that is the accepted F6 cost, not a failure.</precondition>
  <action>
    Capture the phase-base commit first: git rev-parse HEAD > .planning/phases/08-exact-occurrence-ranges/PHASE_BASE — the protected-file gate in Task 3 diffs the working tree against this commit because git status cannot see already-committed edits. Do not commit PHASE_BASE.

    Edit Package.swift (research D1): add `.package(url: "https://github.com/swiftlang/swift-syntax.git", exact: "602.0.0")` to dependencies, and add `.product(name: "SwiftSyntax", package: "swift-syntax")` plus `.product(name: "SwiftParser", package: "swift-syntax")` to the executable target's dependencies. The products land on the executable target; the test target builds @testable import scip_swift so the refiner is reachable from tests without a separate product dependency — if the test target also fails to resolve, add the same two products to the test target rather than reaching for anything else. Run swift package resolve and confirm Package.resolved pins swift-syntax 602.0.0 exactly (603+ must never appear — majors track toolchains, research D1). Run swift build once to verify the toolchain compiles it; the ~261s cold build is expected (F6) — budget the time, do not interrupt it.

    Package legitimacy is pre-approved by the research's manual audit (swiftlang org, GPG-signed release, tag verified via git ls-remote — Package Legitimacy Audit table in 08-RESEARCH.md); no checkpoint needed.

    README Known limitations: update the "Approximate occurrence ranges" bullet to state that occurrence ends are now exact token extents via SwiftSyntax, with name-length approximation remaining as fallback for unparseable regions, and note the ~24 MB binary size accepted for this milestone (research Pitfall 5 — distribution trade-off documented for the release-notes owner).
  </action>
  <verify>
    <automated>set -o pipefail; swift package resolve && grep -A5 '"swift-syntax"' Package.resolved | grep -F '602.0.0' && swift build 2>&1 | tail -3</automated>
  </verify>
  <done>
    Package.resolved pins 602.0.0; swift build compiles against the pinned toolchain; README documents the exact-range behavior and accepted binary-size trade-off.
  </done>
</task>

<task type="tracer" tdd="true">
  <name>Task 2: End-to-end "one fixture file's occurrences emit exact ends in a real .scip" — SwiftSyntaxRefiner + builder wiring, one path only</name>
  <files>Sources/scip-swift/SourceRefinement/SwiftSyntaxRefiner.swift, Sources/scip-swift/SCIPMapping/PositionMapping.swift, Sources/scip-swift/SCIPMapping/SCIPIndexBuilder.swift, Tests/scip-swiftTests/IntegrationTests.swift, Tests/scip-swiftTests/SwiftSyntaxRefinerTests.swift, Tests/scip-swiftTests/PositionMappingTests.swift</files>
  <precondition>Task 1 landed: swift-syntax 602.0.0 resolves and builds (init?(filePath:) needs import SwiftSyntax/SwiftParser to compile).</precondition>
  <behavior>
    - Line-table math: a fixture source's bytes convert offsets to (line, utf8Column) and back; 1-based↔0-based conversions exact on multi-byte lines (08-VALIDATION row "Line-start byte table")
    - Exact-extent invariant: identifier tokens' ends equal start + text.utf8.count; trailing-trivia regression — token `emoji` on `let emoji = "🦖"` ends at 0-based col 9, never 10 (research F1/Pitfall 1)
    - F4 Unicode rows (0-based byte cols): `emoji` def L0 [4,9); `名前` def L1 [4,10); `greet` ref L1 [13,18); `greet` def L3 [5,10); `name` param def L3 [11,15); `String` refs L3 [17,23) and [28,34) (research F4 — use these hand-computed values exactly)
    - Error recovery: source with `struct Broken { let x: = }` still yields `.present` tokens for the valid regions (ok, Broken, x, alsoOk, print per research F5); tokens inside the garbled region miss the map → exactEndColumn returns nil
    - Anchor-miss fallback: an anchor that no token provides (synthetic fake occurrence location crafted against real source) returns nil from the refiner
    - Unreadable file: init?(filePath:) on a nonexistent path returns nil
    - PositionMapping: exact end used when non-nil; nil → endCharacter equals today's approximate value byte-for-byte; 1-based→0-based start math unchanged
    - Tracer end-to-end: fullPipeline over MiniSwiftPackage asserts at least one getter occurrence (display name `getter:name`) whose range end is the exact token end [13,17) on its line, where the approximation would have produced 24 (+7 drift, research F3a)
  </behavior>
  <read_first>
    - 08-RESEARCH.md — §Code Examples "Refiner core" skeleton (verified against 602.0.0), Decisions D1–D6, §Key Verified Facts F1/F3a/F4/F5, Pitfalls 1–4, §Validation Architecture Wave-0 rows
    - Sources/scip-swift/SCIPMapping/PositionMapping.swift — the current singleLineRange math to preserve verbatim in the new signature (research D3)
    - Sources/scip-swift/SCIPMapping/SCIPIndexBuilder.swift — makeDocument's occurrence loop and the singleLineRange call site (~lines 171-177), the cache-hit branch that skips makeDocument entirely
    - Tests/scip-swiftTests/IntegrationTests.swift — fullPipeline's document/occurrence access pattern and the display-name assertion block
    - Tests/scip-swiftTests/SymbolKindMappingTests.swift — the repo's @Suite/@Test string-description style (imports IndexStoreDB directly for SymbolLocation construction)
    - Fixtures/MiniSwiftPackage/Sources/MiniSwiftPackage/Greeter.swift — the tracer's real source (getter:name anchored at token `name`)
  </read_first>
  <action>
    RED first: create Tests/scip-swiftTests/SwiftSyntaxRefinerTests.swift with @Suite("SwiftSyntaxRefinerTests") covering every <behavior> row except the end-to-end one — inline the F4 Unicode source as a Swift string literal (research D4 fixture content), construct the refiner from a temp file written via the repo's makeTemporaryDirectory pattern or a init?(source:) overload added for tests if the file-based init makes inline sources awkward. Craft the anchor-miss case by querying a (line, utf8Column) that falls between two real token starts (e.g. inside an operator or whitespace) against real parsed source. Also create Tests/scip-swiftTests/PositionMappingTests.swift asserting the exact-when-present / approximate-when-nil contract using an IndexStoreDB SymbolLocation constructed directly (public initializer: path, timestamp, moduleName, line, utf8Column — verified in the checkout source). Run both filters — everything fails to compile or fails red (module absent). Commit as test(08-01).

    GREEN, three edits in one commit:
    1. New struct SwiftSyntaxRefiner at Sources/scip-swift/SourceRefinement/SwiftSyntaxRefiner.swift per the research skeleton: failable init?(filePath:) — String(contentsOfFile:encoding:.utf8), nil on failure; build lineStarts over Array(source.utf8) scanning for 0x0A; single Parser.parse(source:) call; iterate tokens(viewMode: .sourceAccurate); end = positionAfterSkippingLeadingTrivia.utf8Offset + token.text.utf8.count — this is the verified invariant, endPosition includes trailing trivia (F1); store map [line(1-based): [0-based byte startCol: 0-based byte endCol]] keyed on ALL tokens not just identifiers (research D5). exactEndColumn(line:utf8Column:) -> Int? takes IndexStoreDB's 1-based values and subtracts 1 from the column on lookup. A WHY comment is warranted and expected at exactly two sites: the endPosition-includes-trailing-trivia invariant and the why-line-start-table-not-SourceLocationConverter note (research D1/D2 — converter column basis unverified, UTF-16 bias pitfall 2). Shape the struct so Phase 9 can extend it with doc extraction from the same parse (store the parsed tree or the source, do not discard it) but extract no docs now (Pitfall 4).
    2. PositionMapping: change signature to singleLineRange(location:displayName:exactEndColumn: Int32?) — zeroBasedLine/zeroBasedStartCharacter math stays verbatim; when exactEndColumn is non-nil, endCharacter = that value; else the existing approximateTokenLength path unchanged. Default the new parameter so existing call sites compile, or update them — one lookup call site exists.
    3. SCIPIndexBuilder.makeDocument: after the occurrences guard, construct let refiner = SwiftSyntaxRefiner(filePath: filePath) (nil-safe — unreadable file means nil refiner means approximation). At the singleLineRange call site, pass exactEndColumn: refiner?.exactEndColumn(line: occurrence.location.line, utf8Column: occurrence.location.utf8Column).map(Int32.init). No other builder change — the cache branch already skips makeDocument on hits, so cached docs carry exact ranges with no re-parse (research D2).
    Then extend IntegrationTests.fullPipeline with the tracer assertion from <behavior> — find the getter:name occurrence (displayName == "getter:name" in document.symbols or the corresponding occurrence's symbol) and assert its singleLineRange end equals the exact token end, plus assert the pre-change approximation would have differed (the drift proof). Note MiniSwiftPackage's Greeter.swift has `name` property → getter occurrence anchored at token `name`; derive the exact columns from the source (line 2 is `  public let name: String` — verify against your own parse, the research measured drift cases on accessor anchors). Run swift test --filter IntegrationTests green, commit as feat(08-01).

    Run both unit filters green. Do not modify Sources/scip-swift/SCIPMapping/PositionMappingTests expectations that encode today's approximation — PositionMappingTests is new in this task; the approximation rows in it must be byte-identical to what today's code produces (fixture for nil exactEndColumn), and the file stays unchanged in 08-02 (protected-file gate).
  </action>
  <verify>
    <automated>swift test --filter SwiftSyntaxRefinerTests && swift test --filter PositionMappingTests && swift test --filter IntegrationTests && swift test --filter RelationSpikeTests</automated>
  </verify>
  <done>
    Refiner unit corpus green (line table, exact-extent invariant, trailing-trivia regression, F4 Unicode rows, error-node tokens, anchor-miss nil, unreadable nil); PositionMapping exact/approximate contract green; fullPipeline's MiniSwiftPackage .scip carries exact ends with the getter drift case asserted end-to-end; RelationSpikeTests (a second real pipeline consumer) stays green.
  </done>
</task>

<task type="auto">
  <name>Task 3: Wave gate — protected files + non-xcodebuild partition</name>
  <files>.planning/phases/08-exact-occurrence-ranges/PHASE_BASE</files>
  <action>
    Run the protected-file gate — the gate protects what must NOT change, and it uses two bases. Files that existed before this phase diff against the commit captured in .planning/phases/08-exact-occurrence-ranges/PHASE_BASE. Tests/scip-swiftTests/PositionMappingTests.swift is created by Task 2 of this plan, so PHASE_BASE predates it — gate it against HEAD instead (proving it is unchanged since the Task 2 commit). The protected surface, identical to the <verify> command's file list: Tests/scip-swiftTests/PositionMappingTests.swift, Tests/scip-swiftTests/SCIPSymbolFormatterTests.swift, Tests/scip-swiftTests/IncrementalIntegrationTests.swift, Tests/scip-swiftTests/ScipIndexMergerTests.swift, Tests/scip-swiftTests/MultiRepoMergeIntegrationTests.swift, Tests/scip-swiftTests/RelationSpikeTests.swift. PositionMapping.swift IS modified this phase (D3 signature change) — do not protect it. If any protected file shows a diff against its gate base, revert the edit — approximation rows and identity/merge/relation guards are this phase's untouched regression surface.
    Then run the full non-xcodebuild partition green (this also pulls in the 261s-cold-build dependency compile — expected once).
  </action>
  <verify>
    <automated>swift test --skip Xcode && git diff --quiet "$(cat .planning/phases/08-exact-occurrence-ranges/PHASE_BASE)" -- Tests/scip-swiftTests/SCIPSymbolFormatterTests.swift Tests/scip-swiftTests/IncrementalIntegrationTests.swift Tests/scip-swiftTests/ScipIndexMergerTests.swift Tests/scip-swiftTests/MultiRepoMergeIntegrationTests.swift Tests/scip-swiftTests/RelationSpikeTests.swift && git diff --quiet HEAD -- Tests/scip-swiftTests/PositionMappingTests.swift</automated>
  </verify>
  <done>
    swift test --skip Xcode fully green (125+ pre-phase tests plus the two new suites); no protected suite modified relative to its gate base (PHASE_BASE for pre-phase suites, HEAD for PositionMappingTests).
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|-------------|-------------|
| Repo source files → parser | Untrusted user-repo Swift source crosses into SwiftSyntax's in-process parser |
| Filesystem → refiner init | arbitrary paths from SwiftFileDiscovery feed String(contentsOfFile:) |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-08-01 | DoS | SwiftSyntax parse of untrusted repo source | low | mitigate | Memory-safe non-throwing parser (research V5); Parser.parse never throws (F5); measured 27ms/118KB (F6) so no pathological runtime; a nil init on read failure keeps indexing alive |
| T-08-02 | Tampering | swift-syntax dependency supply chain | high | mitigate | Exact pin 602.0.0 in Package.resolved (research D1); pre-approved manual legitimacy audit (swiftlang org, GPG-signed tag, git ls-remote verified); pin blocks 603+ drift |
| T-08-03 | DoS | 3.5× binary size / 261s cold build slowing CI | low | accept | Accept recurring per-run CI cost (~154-261s): ci.yml has no caching and runners are ephemeral, so the swift-syntax compile repeats on every run — accepted per research D1/Pitfall 5 with owner sign-off; documented in README trade-off note |
| T-08-04 | Information Disclosure | None new — no auth/session/crypto/network surface change (research V2/V3/V4/V6: no) | low | accept | Local CLI over local source; no new data leaves the machine |
</threat_model>

<verification>
- swift package resolve + Package.resolved contains 602.0.0 (research D1 — exact pin, never from:)
- swift test --filter SwiftSyntaxRefinerTests / PositionMappingTests — fast pure-unit gates per 08-VALIDATION (run after every task commit)
- swift test --filter IntegrationTests && swift test --filter RelationSpikeTests — end-to-end tracer gates
- Protected-file gate: git diff --quiet over the identity suite, incremental byte-identity, both merge suites, and RelationSpikeTests (vs PHASE_BASE); PositionMappingTests vs HEAD (created this plan)
- Wave-1 gate: swift test --skip Xcode — full non-xcodebuild partition green before 08-02 starts
</verification>

<success_criteria>
- swift-syntax 602.0.0 exact-pinned, resolved, compiling against the 6.2.4 toolchain (research D1)
- SwiftSyntaxRefiner exists with the verified invariant math, Unicode byte-column corpus green, trailing-trivia regression green (RANGE-02 unit level, RANGE-01 invariant)
- PositionMapping uses exact end when present and today's approximation when nil (RANGE-03 fallback contract, testable pure-unit)
- MiniSwiftPackage .scip emits exact ends on the getter drift case end-to-end (RANGE-01 tracer)
- All pre-phase suites green; protected files untouched; PHASE_BASE captured
</success_criteria>

<output>
Create `.planning/phases/08-exact-occurrence-ranges/08-01-SUMMARY.md` when done
</output>
