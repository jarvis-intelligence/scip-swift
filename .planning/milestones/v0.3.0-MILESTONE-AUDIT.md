---
milestone: v0.3.0
milestone_name: Readable Indexes
audit_date: 2026-08-18
status: passed
closeout_type: verified_closeout_after_w2_fix
integration_check: .planning/MILESTONE-INTEGRATION-CHECK.md (PASSED, 7 warnings, 0 blockers)
---

# v0.3.0 Milestone Audit — Readable Indexes

## Scope

Phases 6–9, all four `status: passed` in their VERIFICATION.md files. Requirements: REPAIR-01..03, SYMBOL-01..04, RANGE-01..03, DOCS-01..03 (13 total, all `[x]` in REQUIREMENTS.md).

## Requirements Coverage (3-Source Cross-Reference)

| REQ | Phase | VERIFICATION.md | REQUIREMENTS.md | Final |
|-----|-------|-----------------|-----------------|-------|
| REPAIR-01 | 6 | passed, evidence | [x] | satisfied |
| REPAIR-02 | 6 | passed, evidence | [x] | satisfied |
| REPAIR-03 | 6 | passed, evidence | [x] | satisfied |
| SYMBOL-01 | 7 | passed, evidence | [x] | satisfied |
| SYMBOL-02 | 7 | passed, evidence | [x] | satisfied |
| SYMBOL-03 | 7 | passed at phase time; **text now stale vs. current code (W1)** | [x] | satisfied-with-annotation |
| SYMBOL-04 | 7 | passed at phase time; **weakened on warm cache (W2)** | [x] | satisfied-with-gap |
| RANGE-01..03 | 8 | passed, evidence | [x] | satisfied |
| DOCS-01..03 | 9 | passed, evidence | [x] | satisfied |

SUMMARY frontmatter carries `requires:` (plan deps) rather than `requirements-completed`; requirement evidence lives in the VERIFICATION.md tables instead — coverage established via those plus the checkbox state. No orphaned requirements: every REQ-ID appears with evidence in its phase's VERIFICATION.md.

## Integration Check

Full report: `.planning/MILESTONE-INTEGRATION-CHECK.md`. 11/11 phase exports wired and live-verified; all 4 cross-phase E2E flows complete (Xcode × demangle × ranges × docs in one lint-clean run; merge dedup with all features; cache round-trips; destination no-op on SwiftPM). 235 tests green across 9 partitions.

## Findings Requiring Decision (from integration check)

### W1 — SYMBOL-03 text stale after post-milestone rewrite (documentation gap)
Commits `ee2f2d2`/`ac18a3d` (v1.0-track, Aug 18, out-of-band of v0.3.0 phases) replaced raw-USR-embedding canonical symbols with descriptor-chain symbols (`symbolFormatVersion: 2`). Identity, dedup, and cache gating were verified intact live. But REQUIREMENTS.md SYMBOL-03's literal text ("the wrapped USR stays the canonical `symbol` field") no longer matches the code. **Action: re-baseline the requirement text or annotate it at close — a docs decision, not a code regression.**

### W2 — demangle mode is not a cache key (real cross-phase bug)
Proven both directions: `--no-demangle` over a warm demangled cache serves demangled docs; demangle-on over a `--no-demangle` cache serves opaque ones. Cold-cache runs correct; deterministic; lint-clean. Weakens SYMBOL-04 (flag parity) on warm caches only. **Fix is a manifest/cache-key addition (include demangle mode). Recommend fixing in the v1.0 track that owns the current cache code, or as an immediate quick task if v0.3.0 tags first.**

### Minor (W3–W7, documented in integration report)
- W3: `index-many` exposes neither `--no-demangle` nor `--destination`
- W4: `docs/CONFIGURATION.md` doesn't document the v0.3.0 flags
- W5: forced `--build-tool xcodebuild --destination` on SwiftPM errors with misleading text (auto-detect path is a clean no-op)
- W7: shared `--cache-dir` across repos thrashes (repo-global overload fingerprint); correctness preserved

## Out-of-Band Work Note

~21 commits (02-01..02-04 series: USR parser/mapper stack, OverloadTable, symbolFormatVersion cache gating, SchemeFixture goldens, CI toolchain pin; 212+ tests) landed on main AFTER the v0.3.0 phases closed, apparently from a parallel v1.0-track effort (branches `gsd/v1.0-milestone`, `gsd/v0.3.0-milestone` exist). These are NOT v0.3.0 scope. The integration checker audited v0.3.0 flows against the CURRENT tree and confirmed they survived the rewrite (except W1's mechanism change and W2's cache interaction).

## Verdict

**passed** (updated 2026-08-18) — W2 fixed in `5bff6be`: demangle mode now joins the cache manifest key (TDD: RED manifest test → GREEN field + both-direction E2E verification → full partition union 216 tests green: 194 + 19 + 3). W1 re-baselined below; W3–W7 remain documented minor items deferred to the v1.0 track.

### W1 resolution (re-baseline)
SYMBOL-03's mechanism text is superseded by the v1.0-track canonical-symbol rewrite (`symbolFormatVersion: 2`): the canonical `symbol` field now carries descriptor-chain symbols, with the raw USR retained as the fallback for unparseable USRs. The requirement's substance — stable identity, byte-identical second runs, merge dedup keyed on canonical strings — was verified intact against the current tree by the integration checker. Requirement text re-baselined accordingly in REQUIREMENTS.md.
