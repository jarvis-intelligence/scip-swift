---
phase: 03-incremental-indexing
plan: 02
type: execute
wave: 2
depends_on: ["03-01"]
files_modified:
  - Sources/scip-swift/Commands/IndexCommand.swift
  - Sources/scip-swift/SCIPMapping/SCIPIndexBuilder.swift
  - Sources/scip-swift/Build/SwiftPMBuildRunner.swift
  - Sources/scip-swift/Build/BuildError.swift
  - Tests/scip-swiftTests/IncrementalIntegrationTests.swift
  - .gitignore
autonomous: true
requirements: [INCR-01, INCR-02, INCR-04, INCR-05, INCR-06, TEST-04]
estimate:
  tokens: 50000
  raw_tokens: 50000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - A second scip-swift run on an unchanged repo with --cache-dir reuses cached Scip_Document protobufs and produces an identical .scip output (per INCR-02, INCR-03, INCR-04, TEST-04)
    - Modifying one source file causes only that file's Scip_Document to be regenerated; all other cached documents are reused (per INCR-04, TEST-04)
    - Changing the toolchain version or scip-swift version invalidates the entire cache — no stale documents are served (per INCR-06)
    - --index-only mode skips the build step entirely and reads an existing IndexStore from the cache directory's build-scratch (per INCR-05)
    - IndexStoreDB database path is persistent across runs when caching is enabled, not ephemeral (per INCR-01)
    - external_symbols is recomputed correctly across all documents (cached + fresh) — the emitted index passes scip lint with no unresolved symbol errors
    - Without --cache-dir, the pipeline behaves exactly as before (ephemeral temp directory, no caching) — backward compatible
  artifacts:
    - path: "Sources/scip-swift/Commands/IndexCommand.swift"
      provides: "CLI flags --cache-dir and --index-only, persistent path resolution"
      contains: "cacheDir"
    - path: "Sources/scip-swift/SCIPMapping/SCIPIndexBuilder.swift"
      provides: "Cache-aware build loop with cache-hit/miss per file, post-assembly external_symbols recomputation"
      contains: "cacheStore"
    - path: "Sources/scip-swift/Build/SwiftPMBuildRunner.swift"
      provides: "findIndexStore made accessible for --index-only reuse"
    - path: "Sources/scip-swift/Build/BuildError.swift"
      provides: "Cache-specific error case for missing IndexStore in --index-only mode"
    - path: "Tests/scip-swiftTests/IncrementalIntegrationTests.swift"
      provides: "Integration test: index twice, verify cache hit, modify file, verify selective regeneration"
    - path: ".gitignore"
      provides: ".scip-cache/ entry to prevent accidental commits"
  key_links:
    - IndexCommand resolves --cache-dir (or default <repo>/.scip-cache/) and passes CacheStore to SCIPIndexBuilder
    - SCIPIndexBuilder.build() checks ContentHasher.sha256Hex + dateOfLatestUnitFor before calling makeDocument(); on cache hit, loads from CacheStore
    - SCIPIndexBuilder.build() recomputes external_symbols by scanning all documents' occurrences post-assembly (not via makeDocument side-effects alone)
    - IndexCommand uses persistent scratch path (<cacheDir>/build-scratch) and persistent database path (<cacheDir>/index-db) when caching is enabled
    - --index-only mode resolves the index store path from persistent build-scratch via SwiftPMBuildRunner.findIndexStore and skips produceIndexStore()
---

<objective>
Tracer slice: wire the cache primitives (Plan 03-01) into the full pipeline end-to-end. Add --cache-dir and --index-only CLI flags (INCR-02, INCR-05), make paths persistent (INCR-01), integrate cache check/miss logic into SCIPIndexBuilder.build() (INCR-04), solve the external_symbols recomputation problem when loading cached documents, and write the integration test proving cache correctness (TEST-04).

Purpose: This is the largest architectural change in the project — scip-swift goes from stateless to persistent. The tracer proves the full path: CLI flag resolution → persistent build scratch → persistent IndexStoreDB → per-file cache check → cached document load or fresh makeDocument → post-assembly external_symbols recomputation → identical .scip output on second run. The critical correctness challenge is external_symbols: when documents are loaded from cache, the referencedSymbols/systemReferencedSymbols side-effects from makeDocument() are not available, so external_symbols must be recomputed by scanning all documents' occurrences after assembly.

Output: Modified IndexCommand, SCIPIndexBuilder, SwiftPMBuildRunner, BuildError; new integration test; .gitignore entry.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-incremental-indexing/03-RESEARCH.md
@.planning/phases/03-incremental-indexing/03-01-SUMMARY.md
@.planning/codebase/CONVENTIONS.md
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Tracer — wire cache into SCIPIndexBuilder.build() with post-assembly external_symbols recomputation (INCR-01, INCR-04)</name>
  <files>
    Sources/scip-swift/SCIPMapping/SCIPIndexBuilder.swift,
    Tests/scip-swiftTests/IncrementalIntegrationTests.swift
  </files>
  <read_first>
    - Sources/scip-swift/SCIPMapping/SCIPIndexBuilder.swift — current build() at lines 17-48 (the loop, referencedSymbols/systemReferencedSymbols tracking, external_symbols computation at lines 40-43); makeDocument() at lines 62-148 (the occurrence loop, definedSymbols, isSystem classification at lines 136-139)
    - Sources/scip-swift/Caching/CacheStore.swift — from Plan 03-01: loadDocument(hash:), saveDocument(_:hash:), loadManifest(), saveManifest(_:), invalidateAll()
    - Sources/scip-swift/Caching/ContentHasher.swift — from Plan 03-01: sha256Hex(of filePath:) throws -> String
    - Sources/scip-swift/Caching/IndexManifest.swift — from Plan 03-01: IndexManifest struct with isCompatibleWith
    - Sources/scip-swift/Generated/Scip.pb.swift — Scip_SymbolRole enum: definition = 1, so an occurrence has a definition role if (occurrence.symbolRoles & Int32(Scip_SymbolRole.definition.rawValue)) != 0
    - Sources/scip-swift/SCIPMapping/SCIPSymbolFormatter.swift — localSymbolString starts with "local ", globalSymbolString starts with "scip-swift "
    - Tests/scip-swiftTests/IntegrationTests.swift — integration test pattern: fixture path via #filePath, SwiftPMBuildRunner with temp scratch, IndexStoreLoader.open, SCIPIndexBuilder construction
    - .planning/phases/03-incremental-indexing/03-RESEARCH.md lines 270-340 — cache integration pattern and external_symbols pitfall (Pitfall 2)
  </read_first>
  <precondition>Plan 03-01 artifacts exist: Sources/scip-swift/Caching/CacheStore.swift, ContentHasher.swift, IndexManifest.swift compile and their tests pass</precondition>
  <behavior>
    - Test "second run on unchanged repo produces identical documents": Build MiniSwiftPackage fixture, index it once with a CacheStore (temp cache dir), capture document count and all symbol strings. Index again with the same cache dir. Assert document count is identical, symbol strings are identical, and the .scip serialized data is byte-identical.
    - Test "cache hit skips makeDocument for unchanged file": After the first run, the cache directory contains a .scipdoc file. On the second run, verify the document is loaded from cache by checking that the document content matches (if makeDocument were skipped incorrectly, the document would be missing).
    - Test "modifying a source file regenerates only that file's document": Index the fixture once. Modify Greeter.swift (add a new function). Rebuild and re-index. Assert the total document count is unchanged (still 1 file), but the new function's symbol appears in the output (cache was invalidated for the changed file).
    - Test "external_symbols is correct with cached documents": Index the fixture twice (second run uses cache). Assert external_symbols array is non-empty and contains system symbols (like Swift standard library types). Assert no symbol referenced in any document's occurrences is missing from either defined symbols or external_symbols.
    - Test "build without CacheStore behaves identically to current pipeline": Construct SCIPIndexBuilder with cacheStore=nil (or not provided). Assert build() produces the same output as before this change — no cache reads or writes occur.
  </behavior>
  <action>
    Modify SCIPIndexBuilder to accept an optional CacheStore. Add a `let cacheStore: CacheStore?` property. Update the initializer to accept it as an optional parameter defaulting to nil, preserving backward compatibility for existing callers that do not pass it.

    In build(), after opening IndexStoreDB, call indexStoreDB.pollForUnitChangesAndWait() to ensure the database reflects current store state (per INCR-04 staleness mitigation, research Pitfall 3).

    Refactor the per-file loop to implement cache check/miss logic. For each filePath:
    1. If cacheStore is non-nil: compute contentHash via ContentHasher.sha256Hex(of: filePath) (use try? — if hash fails, fall through to uncached makeDocument). Check cacheStore.loadDocument(hash: contentHash). If the cached document exists AND indexStoreDB.dateOfLatestUnitFor(filePath: filePath) returns non-nil (file is in the current index store): use the cached document directly (CACHE HIT).
    2. Otherwise (no cache, hash failed, cache miss, or file not in index store): call makeDocument() as before (CACHE MISS). If cacheStore is non-nil and a document was produced, save it via cacheStore.saveDocument(document, hash: contentHash).

    After the loop, recompute external_symbols by scanning ALL assembled documents' occurrences. This replaces the current approach that relies on referencedSymbols/systemReferencedSymbols being populated during makeDocument(). The new approach: iterate all documents in index.documents. For each document, collect defined symbol strings from document.symbols.map(\.symbol). For each document.occurrences entry: if the occurrence's symbol does NOT start with "local " (it is a global symbol) AND it is NOT in the defined symbols set AND it has not already been seen as defined in any other document: construct a minimal Scip_SymbolInformation for it and add to the external symbols collection. Preserve the existing system vs. non-system split: since the cached Scip_Occurrence protobuf does not carry isSystem, classify all external symbols uniformly (the isSystem distinction from Phase 1 META-04 affects ordering only — both system and non-system externals end up in external_symbols). Deduplicate by symbol string. Sort alphabetically. Assign to index.externalSymbols.

    IMPORTANT: The makeDocument() side-effects on referencedSymbols/systemReferencedSymbols still run for cache-miss files. For correctness, the post-assembly scan must be the authoritative source for external_symbols — it sees all documents (cached + fresh). The existing referencedSymbols/systemReferencedSymbols dictionaries become unused for the external_symbols computation but remain available inside makeDocument for any internal logic. Do NOT remove them from makeDocument's signature — that would break the existing test callers.

    Write IncrementalIntegrationTests.swift as @Suite("Incremental Indexing") following the IntegrationTests pattern. Each test builds the MiniSwiftPackage fixture, creates a temp cache directory, constructs a CacheStore, and runs SCIPIndexBuilder.build() twice. Use defer to clean up temp directories and the fixture .build directory.

    Run swift test --filter IncrementalIntegrationTests to verify the tracer works end-to-end.
  </action>
  <verify>
    <automated>swift build && swift test --filter IncrementalIntegrationTests 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - SCIPIndexBuilder accepts an optional CacheStore parameter (defaulting to nil) — existing callers (IntegrationTests) compile without changes
    - build() checks cacheStore.loadDocument + dateOfLatestUnitFor before makeDocument when cacheStore is provided
    - build() saves freshly computed documents to cacheStore after makeDocument
    - external_symbols is recomputed by scanning ALL documents' occurrences post-assembly — not solely from makeDocument side-effects
    - Second run on unchanged fixture produces identical .scip output (serializedData matches)
    - Modifying a source file causes only that file to be reprocessed
    - build() with cacheStore=nil produces output identical to pre-change behavior
    - indexStoreDB.pollForUnitChangesAndWait() is called after opening the database
    - swift build succeeds; swift test --filter IncrementalIntegrationTests passes
    - swift test --filter IntegrationTests passes (no regression in existing integration test)
  </acceptance_criteria>
  <done>
    The cache-aware build loop works end-to-end: second run reuses cached documents, modified files are regenerated, external_symbols is correct across cached + fresh documents, and backward compatibility is preserved.
  </done>
  <reversibility rating="costly">CacheStore integration touches the core build loop's external_symbols computation, which other phases (Phase 4 cross-repo) will also read. Removing the cache integration restores the loop, but callers in IndexCommand must also be reverted. The post-assembly external_symbols scan is additive — it can coexist with the old approach behind a conditional.</reversibility>
</task>

<task type="auto">
  <name>Task 2: CLI flags --cache-dir and --index-only + persistent paths (INCR-02, INCR-05, INCR-01)</name>
  <files>
    Sources/scip-swift/Commands/IndexCommand.swift,
    Sources/scip-swift/Build/SwiftPMBuildRunner.swift,
    Sources/scip-swift/Build/BuildError.swift,
    .gitignore
  </files>
  <read_first>
    - Sources/scip-swift/Commands/IndexCommand.swift — current run() at lines 29-44, produceIndexStore() at lines 48-75, makeTemporaryDirectory() at lines 77-81
    - Sources/scip-swift/Build/SwiftPMBuildRunner.swift — scratchPath property at line 6, findIndexStore as private static at lines 38-49
    - Sources/scip-swift/Build/BuildError.swift — exhaustive error enum pattern (6 cases with structured context)
    - Sources/scip-swift/Platform/ToolchainInfo.swift — pinnedSwiftVersion = "6.2.4"
    - Sources/scip-swift/Version.swift — ScipSwiftVersion.version = "0.1.2"
    - Package.resolved — indexstore-db revision c993f4fb4f321fae1945e96a2377742f24e132f4
    - .gitignore — current entries (.build/, .swiftpm/, *.scip, etc.)
    - .planning/phases/03-incremental-indexing/03-RESEARCH.md lines 420-510 — CLI flag patterns, persistent path resolution, --index-only validation
  </read_first>
  <precondition>Task 1 is complete — SCIPIndexBuilder accepts optional CacheStore and its integration test passes</precondition>
  <action>
    Add two CLI options to IndexCommand following the existing ArgumentParser pattern:
    - `@Option(name: .long, help: "Directory for the incremental index cache. Defaults to <repo>/.scip-cache/.") var cacheDir: String?`
    - `@Flag(name: .long, help: "Skip the build step and read an existing IndexStore directly.") var indexOnly: Bool = false`

    In run(), resolve the cache directory path: if --cache-dir is provided, use it (resolve to absolute via URL.standardizedFileURL.path); if --index-only is set, use the default cache dir even if --cache-dir was not explicitly given (because --index-only requires persistent paths to find the prior build output). The default cache dir is `<resolvedRepoPath>/.scip-cache`.

    Determine whether persistent paths should be used: persistent caching is active when cacheDir is non-nil OR indexOnly is true. When persistent: use <cacheDir>/build-scratch for SwiftPMBuildRunner's scratchPath, <cacheDir>/index-db for SCIPIndexBuilder's databasePath. When NOT persistent (no flags): use the existing ephemeral temp directory behavior (backward compatible).

    For --index-only mode (INCR-05): if indexOnly is true, skip produceIndexStore() entirely. Instead, resolve the index store path from the persistent build-scratch directory. Make SwiftPMBuilduildRunner.findIndexStore accessible by changing it from `private static` to `internal static` so IndexCommand can call it. If the index store is not found at the expected path, throw a new BuildError case: `.indexStoreNotFoundForIndexOnly(expectedPath: String)` with a message explaining the user must run without --index-only first. If indexOnly is true but the build-scratch directory does not exist at all, the same error applies.

    Add the new BuildError case to the exhaustive switch in BuildError.description. Follow the existing pattern: a descriptive multi-line string explaining what happened and how to fix it. Reference that --index-only requires a prior indexed build in the cache directory.

    Construct CacheStore when caching is active: create the cache directory with FileManager.createDirectory(withIntermediateDirectories: true). Load the manifest via cacheStore.loadManifest(). If the manifest exists, check isCompatibleWith against the current versions (ToolchainInfo.pinnedSwiftVersion, ScipSwiftVersion.version, indexstore-db revision from Package.resolved, and the detected build tool name). If incompatible: call cacheStore.invalidateAll() to clear stale data, then create a fresh manifest. Save the updated manifest after the build completes.

    Pass the CacheStore to SCIPIndexBuilder's initializer (the cacheStore parameter added in Task 1). When caching is not active, pass nil.

    Add `.scip-cache/` to .gitignore to prevent accidental commits of cache data.

    For the indexstore-db revision string in the cache key: read it from the hardcoded constant. Since Package.resolved is not available at runtime in the binary, hardcode the revision hash as a static constant. This is acceptable because it changes only when SPM dependencies are updated, at which point the developer updates the constant. Place it in a new private static property on IndexCommand or inline in the manifest construction.

    Verify backward compatibility: run swift test --filter IntegrationTests — it must still pass because when no flags are given, the pipeline uses the ephemeral temp directory as before.
  </action>
  <verify>
    <automated>swift build && swift test 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - IndexCommand has @Option cacheDir and @Flag indexOnly following existing ArgumentParser conventions
    - When neither flag is given, the pipeline uses ephemeral temp directories (backward compatible — IntegrationTests passes unchanged)
    - When --cache-dir is given: persistent scratch path, persistent database path, CacheStore constructed and passed to SCIPIndexBuilder
    - When --index-only is given: produceIndexStore() is skipped, index store path resolved from persistent build-scratch
    - --index-only with no prior build produces a clear BuildError explaining the user must run without --index-only first
    - SwiftPMBuildRunner.findIndexStore is accessible from IndexCommand (changed from private to internal)
    - BuildError has a new case for missing index store in --index-only mode, with an actionable description
    - Cache manifest is loaded, version-checked, invalidated if incompatible, and saved after build
    - .gitignore contains .scip-cache/
    - swift test passes (all existing tests + new IncrementalIntegrationTests)
  </acceptance_criteria>
  <done>
    CLI flags --cache-dir and --index-only are wired end-to-end: persistent paths, cache version checking, --index-only skips build and reads existing IndexStore, backward compatibility preserved.
  </done>
  <reversibility rating="reversible">CLI flags are additive — omitting them restores the original behavior. The persistent/ephemeral path decision is a single conditional branch. Removing the flags and reverting findIndexStore to private restores the original code exactly.</reversibility>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Integration test — index → modify → re-index → verify cache correctness (TEST-04, INCR-06)</name>
  <files>
    Tests/scip-swiftTests/IncrementalIntegrationTests.swift
  </files>
  <read_first>
    - Tests/scip-swiftTests/IncrementalIntegrationTests.swift — tests written in Task 1 (tracer)
    - Tests/scip-swiftTests/IntegrationTests.swift — fixture path resolution and temp directory patterns
    - Sources/scip-swift/Commands/IndexCommand.swift — modified in Task 2 (--cache-dir, --index-only, manifest version checking)
    - .planning/phases/03-incremental-indexing/03-RESEARCH.md lines 596-620 — TEST-04 test map and Wave 0 gap definition
  </read_first>
  <precondition>Tasks 1 and 2 are complete — cache integration works end-to-end and CLI flags are wired</precondition>
  <behavior>
    - Test "version mismatch invalidates cache": Index fixture once with cache dir. Manually write a manifest with a wrong toolchainVersion to the cache dir. Index again. Assert the cache was invalidated — the .scipdoc files were regenerated (check file modification times or manifest reset).
    - Test "different converter version invalidates cache": Same setup but change converterVersion in the manifest. Assert invalidation.
    - Test "index-only mode reads existing store": Build the fixture with persistent cache dir (using SwiftPMBuildRunner with persistent scratch). Run SCIPIndexBuilder.build() with the index store from that scratch (simulating --index-only). Assert documents are produced correctly — the index store was read without a new build.
    - Test "index-only with no prior build produces clear error": Point --index-only at a cache dir with no build-scratch. Assert BuildError is thrown (the indexStoreNotFoundForIndexOnly case).
  </behavior>
  <action>
    Extend IncrementalIntegrationTests.swift with the new @Test cases described in behavior. These tests exercise the version invalidation and --index-only paths that were wired in Tasks 1 and 2.

    For the version mismatch tests: after the first successful indexing run, overwrite the manifest.json in the cache dir with a manifest containing a wrong version field (load via JSONDecoder, mutate the field, re-encode via JSONEncoder). On the next run, the manifest check should detect the mismatch and call invalidateAll, clearing the cache. Verify by checking that the manifest was rewritten with correct versions after the second run, or that the .scipdoc files have fresh timestamps.

    For the --index-only tests: construct the test to first do a full build with a persistent cache dir (using SwiftPMBuildRunner directly with a persistent scratchPath), then run SCIPIndexBuilder with the index store path resolved from that scratch via SwiftPMBuildRunner.findIndexStore. Assert the build produces valid documents. For the error case, point findIndexStore at a nonexistent scratch path and assert the BuildError is thrown.

    Run swift test to verify all tests pass — the full suite including existing 57+ tests plus all incremental indexing tests.
  </action>
  <verify>
    <automated>swift test 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - Tests/scip-swiftTests/IncrementalIntegrationTests.swift contains tests for: second-run cache hit (from Task 1), version mismatch invalidation (toolchainVersion + converterVersion), --index-only mode reading existing store, --index-only error on missing store
    - swift test passes — full suite green, no regressions
    - Version mismatch tests confirm that changing any version field triggers cache invalidation
    - --index-only test confirms the existing index store is read without rebuilding
  </acceptance_criteria>
  <done>
    Integration test suite confirms: cache reuse on unchanged files, selective regeneration on modified files, version-based invalidation, and --index-only mode correctness. Full test suite passes.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user→CLI | --cache-dir and --index-only accept user-provided paths |
| disk→pipeline | Persistent IndexStoreDB and cache files read from disk across runs |
| build→IndexStoreDB | Incremental build may leave partial state in persistent scratch |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-03 | Tampering | --cache-dir path traversal | medium | mitigate | Resolve to absolute via URL(fileURLWithPath:).standardizedFileURL.path (already done for repoPath; apply same pattern to cacheDir). Reject paths that resolve outside the expected location. |
| T-03-04 | Tampering | Symlink attack on cache directory | low | accept | Cache contains only derived protobuf data (no source, no secrets). Even if a symlink redirects the cache, the worst case is a cache miss (data is recomputed from the IndexStore). No privilege escalation path exists. |
| T-03-05 | Tampering | Partial build leaves incomplete IndexStore | high | mitigate | pollForUnitChangesAndWait() called after opening IndexStoreDB ensures the database reflects current store state. dateOfLatestUnitFor(filePath:) returns nil for files not in the store — these are treated as cache misses (not silently skipped). Build exit code is validated (existing check in SwiftPMBuildRunner). |
| T-03-06 | Information Disclosure | .scip-cache/ accidentally committed | medium | mitigate | .gitignore entry added in Task 2 prevents accidental commits. Cache contains only version strings, hashes, and SCIP protobufs — no source code or secrets. |
| T-03-SC | Tampering | Package installs | N/A | accept | This plan installs zero external packages. No package legitimacy risk. |
</threat_model>

<verification>
1. swift build — project compiles with cache integration
2. swift test —filter IncrementalIntegrationTests — cache hit/miss, version invalidation, --index-only all pass
3. swift test —filter IntegrationTests — existing integration test still passes (backward compatible)
4. swift test — full suite passes with no regressions
5. Manual check (optional): run the scip-swift binary twice on MiniSwiftPackage with --cache-dir and diff the .scip outputs — they should be byte-identical
</verification>

<success_criteria>
- Second run on unchanged repo with --cache-dir produces identical .scip output while reusing cached documents
- Modifying one source file regenerates only that file's document; others are reused from cache
- Changing toolchain or scip-swift version invalidates the entire cache
- --index-only mode skips the build and reads an existing IndexStore
- Without flags, the pipeline behaves exactly as before (backward compatible)
- external_symbols is correct across cached + fresh documents (no scip lint unresolved-symbol errors)
- Full test suite passes (existing tests + new incremental tests)
</success_criteria>

<output>
Create `.planning/phases/03-incremental-indexing/03-02-SUMMARY.md` when done
</output>

<artifacts>
This plan produces:
- Modified `Sources/scip-swift/SCIPMapping/SCIPIndexBuilder.swift` — cache-aware build loop with per-file cache check/miss, post-assembly external_symbols recomputation
- Modified `Sources/scip-swift/Commands/IndexCommand.swift` — --cache-dir and --index-only flags, persistent path resolution, manifest version checking
- Modified `Sources/scip-swift/Build/SwiftPMBuildRunner.swift` — findIndexStore changed from private to internal static
- Modified `Sources/scip-swift/Build/BuildError.swift` — new indexStoreNotFoundForIndexOnly error case
- `Tests/scip-swiftTests/IncrementalIntegrationTests.swift` — integration tests for cache reuse, selective regeneration, version invalidation, --index-only mode
- Modified `.gitignore` — .scip-cache/ entry
</artifacts>
