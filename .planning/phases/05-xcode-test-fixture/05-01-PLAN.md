---
phase: 05-xcode-test-fixture
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - Fixtures/XcodeTestProject/.gitkeep
  - Fixtures/XcodeTestProject/scip-swift-test.xcodeproj/project.pbxproj
  - Fixtures/XcodeTestProject/scip-swift-test/SwiftFile.swift
  - Tests/scip-swiftTests/XcodeIntegrationTests.swift
  - .github/workflows/ci.yml
autonomous: false
requirements: [TEST-01]
estimate:
  tokens: 25000
  raw_tokens: 25000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - A minimal .xcodeproj fixture exists under Fixtures/ with at least one Swift source file
    - An integration test builds the fixture with xcodebuild and indexes it end-to-end via SCIPIndexBuilder
    - The test fails if XcodebuildBuildRunner regresses (broken IndexStore discovery, scheme resolution failure)
  artifacts:
    - path: "Fixtures/XcodeTestProject/scip-swift-test.xcodeproj/project.pbxproj"
      provides: "Minimal Xcode project file for integration testing"
    - path: "Tests/scip-swiftTests/XcodeIntegrationTests.swift"
      provides: "End-to-end Xcode integration test"
  key_links:
    - XcodeIntegrationTests uses XcodebuildBuildRunner to build the fixture
    - XcodeIntegrationTests uses SCIPIndexBuilder to index the built IndexStore
---

<objective>
Create a minimal .xcodeproj fixture and an end-to-end integration test that validates the Xcode build path (XcodebuildBuildRunner → IndexStore → SCIPIndexBuilder), closing the last test-coverage gap documented in the roadmap.
</objective>

<tasks>

<task type="auto">
  <name>Task 1: Create minimal Xcode project fixture</name>
  <files>
    Fixtures/XcodeTestProject/scip-swift-test.xcodeproj/project.pbxproj,
    Fixtures/XcodeTestProject/scip-swift-test/SwiftFile.swift
  </files>
  <read_first>
    - Sources/scip-swift/Build/XcodebuildBuildRunner.swift — what arguments are passed (scheme, derivedDataPath, index store enable, code signing disabled)
    - Sources/scip-swift/Build/XcodeProjectLocator.swift — how projects/schemes are resolved
    - Fixtures/MiniSwiftPackage/Package.swift — existing fixture pattern
  </read_first>
  <action>
    Create a minimal macOS command-line tool Xcode project. The project.pbxproj must define:
    - A single target "scip-swift-test" of type com.apple.product-type.tool (macOS command-line tool)
    - One source file: SwiftFile.swift containing a simple class with a method
    - The project must have exactly one scheme so XcodeProjectLocator.resolveScheme auto-detects it
    - Minimum deployment target macOS 14.0 (matching Package.swift)
    - Product name "scip-swift-test"

    SwiftFile.swift content: a simple class with inheritance (class Animal, class Dog: Animal with override func) so the index has meaningful symbols.

    The project.pbxproj is a committed file — it does not need to be generated. It must be a valid Xcode project that `xcodebuild -list` recognizes.
  </action>
  <verify>
    <automated>cd Fixtures/XcodeTestProject && xcodebuild -list 2>&1 | grep "scip-swift-test"</automated>
  </verify>
  <acceptance_criteria>
    - Fixtures/XcodeTestProject/scip-swift-test.xcodeproj/project.pbxproj exists and is valid
    - SwiftFile.swift contains at least one class with a method
    - xcodebuild -list shows the scheme "scip-swift-test"
  </acceptance_criteria>
  <done>
    Minimal Xcode project fixture committed and recognized by xcodebuild.
  </done>
</task>

<task type="auto">
  <name>Task 2: End-to-end Xcode integration test (TEST-01)</name>
  <files>
    Tests/scip-swiftTests/XcodeIntegrationTests.swift,
    .github/workflows/ci.yml
  </files>
  <read_first>
    - Tests/scip-swiftTests/IntegrationTests.swift — existing SwiftPM integration test pattern
    - Sources/scip-swift/Build/XcodebuildBuildRunner.swift — produceIndexStore() and arguments
    - Sources/scip-swift/Build/XcodeProjectLocator.swift — workspaceOrProjectArguments and resolveScheme
    - .github/workflows/ci.yml — current CI config
  </read_first>
  <action>
    Create XcodeIntegrationTests.swift as @Suite("Xcode Integration") with a single @Test that:
    1. Resolves the fixture path via #filePath navigation to Fixtures/XcodeTestProject
    2. Creates a temp work directory with defer cleanup
    3. Uses XcodeProjectLocator.workspaceOrProjectArguments to find the .xcodeproj
    4. Uses XcodeProjectLocator.resolveScheme to get the scheme (should auto-detect "scip-swift-test")
    5. Constructs XcodebuildBuildRunner with the resolved project args, scheme, and derivedDataPath under the temp dir
    6. Calls produceIndexStore() to build with indexing enabled
    7. Constructs SCIPIndexBuilder with the resulting IndexStore path
    8. Calls build() and asserts: at least 1 document, document language is "Swift", symbols are non-empty

    In ci.yml, the Xcode integration test runs as part of `swift test` — no special CI changes needed beyond ensuring the fixture is committed. Optionally add a comment noting the Xcode fixture is tested by the suite.
  </action>
  <verify>
    <automated>swift test --filter XcodeIntegration 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - Tests/scip-swiftTests/XcodeIntegrationTests.swift exists with @Suite("Xcode Integration")
    - Test builds the Xcode fixture with XcodebuildBuildRunner and indexes it end-to-end
    - Test asserts documents.count > 0, language == "Swift", symbols non-empty
    - swift test --filter XcodeIntegration passes
  </acceptance_criteria>
  <done>
    Xcode end-to-end integration test passes, closing the documented test-coverage gap.
  </done>
</task>

</tasks>

<success_criteria>
- Minimal .xcodeproj fixture committed under Fixtures/XcodeTestProject/
- Integration test builds + indexes the fixture via XcodebuildBuildRunner → SCIPIndexBuilder
- Test fails on XcodebuildBuildRunner regression
</success_criteria>
