---
phase: 01-symbol-metadata-enrichment
plan: 02
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - Sources/scip-swift/SCIPMapping/SymbolRoleMapping.swift
  - Tests/scip-swiftTests/SymbolRoleMappingTests.swift
  - Sources/scip-swift/SCIPMapping/SCIPIndexBuilder.swift
autonomous: true
requirements: [META-03, META-02, TEST-03]
estimate:
  tokens: 42000
  raw_tokens: 42000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - Symbols with SymbolProperty.unitTest set carry the SCIP Test role bit (0x20) in their occurrence (per META-03)
    - Occurrences with .declaration role (and NOT .definition) carry the SCIP ForwardDefinition role bit (0x40) (per META-03)
    - Local symbols (SymbolProperty.local) have enclosing_symbol populated from .childOf relation data (per META-02)
    - The SymbolRoleMapping signature change from scipRoles(for:) to scipRoles(for:symbol:) is propagated to all callers and tests (per META-03, TEST-03)
    - No occurrence has both Definition and ForwardDefinition bits set simultaneously (per META-03 scip lint forwardDefIsDefinitionError)
  artifacts:
    - Sources/scip-swift/SCIPMapping/SymbolRoleMapping.swift (modified — extended)
    - Tests/scip-swiftTests/SymbolRoleMappingTests.swift (modified — extended)
    - Sources/scip-swift/SCIPMapping/SCIPIndexBuilder.swift (modified — enclosing_symbol + role call site)
  key_links:
    - SCIPIndexBuilder.makeDocument passes occurrence.symbol to SymbolRoleMapping.scipRoles(for:symbol:) for .unitTest property access
    - SCIPIndexBuilder.makeDocument reads .childOf relation to set symbolInformation.enclosingSymbol on local symbols
    - SymbolRoleMapping defensively guards .declaration mapping to skip when .definition is also present (per RESEARCH.md Pitfall 4)
---

<objective>
Expand SymbolRoleMapping with ForwardDefinition (.declaration), Test (SymbolProperty.unitTest), and Generated bits (META-03), populate enclosing_symbol for local symbols via .childOf relation (META-02), and extend SymbolRoleMappingTests for the new cases (TEST-03). This plan depends on Plan 01-01 because the SCIPIndexBuilder occurrence loop is already modified there — this plan adds further enrichment to the same loop.

Purpose: Test symbols (Swift Testing @Test, XCTestCase subclasses) get the Test role bit so consumers can identify and filter them. Forward declarations (protocol requirements, @objc forward decls) are distinguished from definitions. Local variables/parameters appear nested under their containing scope in symbol outlines.

Output: Extended SymbolRoleMapping with three new role bits, enclosing_symbol population in SCIPIndexBuilder, and comprehensive unit tests for all expanded role cases.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-symbol-metadata-enrichment/01-RESEARCH.md
@.planning/phases/01-symbol-metadata-enrichment/01-PATTERNS.md
@.planning/codebase/CONVENTIONS.md
@.planning/codebase/TESTING.md
@.planning/phases/01-symbol-metadata-enrichment/01-01-SUMMARY.md
</context>

<tasks>

<task type="tdd" tdd="true">
  <name>Task 1: Extend SymbolRoleMapping with ForwardDefinition, Test, Generated bits (META-03, TEST-03)</name>
  <files>
    Sources/scip-swift/SCIPMapping/SymbolRoleMapping.swift,
    Tests/scip-swiftTests/SymbolRoleMappingTests.swift
  </files>
  <read_first>
    - Sources/scip-swift/SCIPMapping/SymbolRoleMapping.swift — current mapper at lines 1-20, signature scipRoles(for indexStoreRoles: SymbolRole) -> Int32
    - Tests/scip-swiftTests/SymbolRoleMappingTests.swift — existing 8 test cases, all call scipRoles(for:) with SymbolRole only
    - Tests/scip-swiftTests/SymbolKindMappingTests.swift — makeSymbol helper pattern (lines 7-13) for constructing Symbol with properties
    - .planning/phases/01-symbol-metadata-enrichment/01-RESEARCH.md lines 242-285 — Pattern 2: Extended SymbolRoleMapping with exact bit values and defensive guard logic
    - .build/checkouts/indexstore-db/Sources/IndexStoreDB/SymbolProperty.swift — SymbolProperty.unitTest at line 24, SymbolProperty.local at line 29, .contains() method at lines 76-82
  </read_first>
  <behavior>
    - Test ".declaration without .definition maps to ForwardDefinition": Call scipRoles(for: .declaration, symbol: makeSymbol(kind: .function)), expect Int32(Scip_SymbolRole.forwardDefinition.rawValue) == 64.
    - Test ".declaration AND .definition together sets only Definition (collision guard)": Call scipRoles(for: [.declaration, .definition], symbol: makeSymbol(kind: .function)), expect ONLY Int32(Scip_SymbolRole.definition.rawValue) == 1, NOT |forwardDefinition. Assert (roles & 64) == 0.
    - Test "SymbolProperty.unitTest sets Test bit": Call scipRoles(for: .definition, symbol: makeSymbol(kind: .instanceMethod, properties: .unitTest)), expect (result & Int32(Scip_SymbolRole.test.rawValue)) != 0 where test == 32.
    - Test "SymbolProperty.unitTest combined with .definition sets both bits": Same as above but assert (result & 1) != 0 AND (result & 32) != 0.
    - Test "non-unitTest symbol does not get Test bit": Call scipRoles(for: .definition, symbol: makeSymbol(kind: .function)), assert (result & 32) == 0.
    - Test "all existing tests still pass after signature change": Update all 8 existing test calls from scipRoles(for: .X) to scipRoles(for: .X, symbol: makeSymbol(kind: .function)) — verify results are unchanged.
  </behavior>
  <action>
    Write the new test cases FIRST (RED). Add a private makeSymbol helper to SymbolRoleMappingTests mirroring SymbolKindMappingTests: `private func makeSymbol(kind: IndexSymbolKind, properties: SymbolProperty = SymbolProperty()) -> Symbol`. The Symbol init takes (usr:name:kind:subKind:properties:language:) — pass usr "s:fake", name "fake", subKind .none (default), the properties parameter, language .swift. Update all 8 existing test calls to include the new `symbol:` parameter. Add the 6 new @Test cases listed in behavior. Run to confirm RED (signature doesn't exist yet).

    Then modify SymbolRoleMapping.swift (GREEN). Change the function signature from `scipRoles(for indexStoreRoles: SymbolRole) -> Int32` to `scipRoles(for indexStoreRoles: SymbolRole, symbol: Symbol) -> Int32`. Add `import IndexStoreDB` is already present. After the existing .definition block, add: if indexStoreRoles.contains(.declaration) AND NOT indexStoreRoles.contains(.definition) — set roles |= Int32(Scip_SymbolRole.forwardDefinition.rawValue). The guard prevents ForwardDefinition+Definition collision (scip lint forwardDefIsDefinitionError per RESEARCH.md Pitfall 4). After the existing read/write block, add: if symbol.properties.contains(.unitTest) — set roles |= Int32(Scip_SymbolRole.test.rawValue). The SymbolProperty.contains method at SymbolProperty.swift:76-82 handles bit-testing correctly. Do NOT implement Generated bit detection yet — IndexStoreDB has no generated flag; the file-path heuristic for Generated requires SCIPIndexBuilder context (file path), which the mapper doesn't receive. Defer Generated to a follow-up if needed.

    Update the file-level doc comment to mention the expanded role set: ForwardDefinition for .declaration, Test for SymbolProperty.unitTest.
  </action>
  <verify>
    <automated>swift test --filter SymbolRoleMapping</automated>
  </verify>
  <acceptance_criteria>
    - SymbolRoleMapping.scipRoles signature is (for indexStoreRoles: SymbolRole, symbol: Symbol) -> Int32
    - .declaration without .definition maps to ForwardDefinition (0x40)
    - .declaration + .definition sets only Definition, not ForwardDefinition (collision guard verified)
    - SymbolProperty.unitTest maps to Test (0x20)
    - All 8 existing tests updated and passing with the new signature
    - swift test --filter SymbolRoleMapping passes all cases
  </acceptance_criteria>
  <done>
    SymbolRoleMapping has three new role mappings with comprehensive tests covering all expanded cases.
  </done>
</task>

<task type="auto">
  <name>Task 2: Update SCIPIndexBuilder call site for new SymbolRoleMapping signature (META-03)</name>
  <files>
    Sources/scip-swift/SCIPMapping/SCIPIndexBuilder.swift
  </files>
  <read_first>
    - Sources/scip-swift/SCIPMapping/SCIPIndexBuilder.swift — current occurrence loop, specifically the scipRoles call at line 89 (modified by Plan 01-01)
    - Sources/scip-swift/SCIPMapping/SymbolRoleMapping.swift — new signature from Task 1: scipRoles(for:symbol:)
  </read_first>
  <action>
    In SCIPIndexBuilder.makeDocument(), update the SymbolRoleMapping call from `SymbolRoleMapping.scipRoles(for: occurrence.roles)` to `SymbolRoleMapping.scipRoles(for: occurrence.roles, symbol: symbol)`. The `symbol` variable is already in scope at that point in the loop (line 78: `let symbol = occurrence.symbol`). This is a one-line change to pass the symbol parameter so the mapper can access symbol.properties for .unitTest detection.

    No other changes in this task — enclosing_symbol wiring is Task 3.
  </action>
  <verify>
    <automated>swift build && swift test --filter "Integration"</automated>
  </verify>
  <acceptance_criteria>
    - SCIPIndexBuilder.makeDocument passes occurrence.symbol to SymbolRoleMapping.scipRoles(for:symbol:)
    - swift build compiles with no errors
    - swift test --filter "Integration" passes (existing integration test still works with the new call)
  </acceptance_criteria>
  <done>
    SCIPIndexBuilder uses the new SymbolRoleMapping signature, enabling Test and ForwardDefinition role bits on occurrences in the output index.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Populate enclosing_symbol for local symbols from .childOf relation (META-02)</name>
  <files>
    Sources/scip-swift/SCIPMapping/SCIPIndexBuilder.swift
  </files>
  <read_first>
    - Sources/scip-swift/SCIPMapping/SCIPIndexBuilder.swift — occurrence loop, specifically the symbolInformation construction and the isLocal check at line 78
    - Sources/scip-swift/SCIPMapping/SCIPSymbolFormatter.swift — globalSymbolString(packageManager:moduleName:usr:) for formatting enclosing symbol strings
    - Sources/scip-swift/SCIPMapping/RelationshipMapping.swift — shows .childOf is excluded from relationships; it goes to enclosing_symbol instead
    - .planning/phases/01-symbol-metadata-enrichment/01-RESEARCH.md lines 282-305 — Pattern 3: Enclosing Symbol from .childOf
    - Tests/scip-swiftTests/RelationSpikeTests.swift — spike test has outerFunction with local let localValue; verify .childOf is populated (from Plan 01-01)
  </read_first>
  <behavior>
    - When a local symbol (SymbolProperty.local) has a .childOf relation in its occurrence.relations, its SymbolInformation.enclosingSymbol is set to the parent symbol's SCIP global string.
    - Non-local symbols do not get enclosingSymbol set (it is only for locals per scip.proto:448-463).
    - Symbols without a .childOf relation get no enclosingSymbol (remains empty/default).
  </behavior>
  <action>
    In SCIPIndexBuilder.makeDocument(), inside the occurrence loop after setting symbolInformation.kind (and after the relationships wiring from Plan 01-01), add enclosing_symbol logic. Only when isLocal is true (symbol.properties.contains(.local)): search occurrence.relations for the first relation where relation.roles.contains(.childOf). If found, set symbolInformation.enclosingSymbol = SCIPSymbolFormatter.globalSymbolString(packageManager: buildToolName, moduleName: occurrence.location.moduleName, usr: childOfRelation.symbol.usr). The enclosing symbol is formatted as a global symbol because enclosing scopes (containing types, functions, modules) are non-local even when the child is local.

    This happens before definedSymbols[symbolString] = symbolInformation so the enclosingSymbol is included when the symbol info is stored. Ensure this logic does not interfere with the relationship post-filter from Plan 01-01 — enclosingSymbol is a separate field on Scip_SymbolInformation, not a Relationship.

    Verify using the RelationSpikeFixture: the local variable `localValue` inside `outerFunction()` should have its enclosingSymbol set to the SCIP global string for outerFunction. If the spike test (Plan 01-01) showed .childOf is not populated for local variables, document this as a known limitation and skip setting enclosingSymbol — the field remains empty, which is valid for scip lint (enclosingSymbol is optional).
  </action>
  <verify>
    <automated>swift build && swift test --filter "Integration"</automated>
  </verify>
  <acceptance_criteria>
    - SCIPIndexBuilder.makeDocument sets symbolInformation.enclosingSymbol for local symbols with .childOf relation
    - Non-local symbols do not get enclosingSymbol set
    - swift build compiles
    - swift test --filter "Integration" passes
    - If .childOf is populated for locals (confirmed by spike), at least one local SymbolInformation in the fixture output has non-empty enclosingSymbol; if not populated, this is documented as a known limitation
  </acceptance_criteria>
  <done>
    Local symbols with .childOf relation data have enclosing_symbol populated in the output index, enabling nested display in symbol outlines.
  </done>
  <reversibility rating="reversible">Enclosing symbol population is additive — removing the .childOf check restores original behavior.</reversibility>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| compiler→IndexStoreDB | Swift compiler output is trusted (local build) |
| IndexStoreDB→SCIP mapping | Symbol properties (.unitTest, .local) and relation roles are compiler-authoritative data |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-01 | Tampering | Symbol properties (unitTest, local) | low | accept | Properties are set by the Swift compiler based on source code declarations — not user-controllable input at the mapping layer. |
| T-02-02 | Tampering | .childOf relation data | low | accept | Relation data is compiler-generated; USRs in enclosing symbols are escaped by SCIPSymbolFormatter. |
| T-02-SC | Tampering | Package installs | N/A | accept | No external packages installed. |
</threat_model>

<verification>
1. swift build — project compiles with extended SymbolRoleMapping and enclosing_symbol wiring
2. swift test --filter SymbolRoleMapping — all unit tests pass including new ForwardDefinition and Test cases
3. swift test --filter "Integration" — integration test passes with the updated role mapping and enclosing symbol
4. Manual: verify no occurrence in fixture output has both Definition and ForwardDefinition bits set
</verification>

<success_criteria>
- Swift Testing / XCTest symbols carry the Test role bit in their occurrences
- override func / .declaration occurrences carry ForwardDefinition (without .definition collision)
- Local symbols with .childOf relation have enclosing_symbol populated
- All SymbolRoleMapping unit tests pass with the new signature
</success_criteria>

<output>
Create `.planning/phases/01-symbol-metadata-enrichment/01-02-SUMMARY.md` when done
</output>

<artifacts>
This plan produces:
- Extended `Sources/scip-swift/SCIPMapping/SymbolRoleMapping.swift` — signature changed to (for:symbol:), adds ForwardDefinition and Test bit mappings
- Extended `Tests/scip-swiftTests/SymbolRoleMappingTests.swift` — new test cases for ForwardDefinition, Test, collision guard; all existing tests updated for new signature
- Modified `Sources/scip-swift/SCIPMapping/SCIPIndexBuilder.swift` — updated role mapping call site and enclosing_symbol population from .childOf
</artifacts>
