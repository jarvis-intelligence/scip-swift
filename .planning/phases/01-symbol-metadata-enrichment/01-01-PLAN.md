---
phase: 01-symbol-metadata-enrichment
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - Fixtures/RelationSpikeFixture/Package.swift
  - Fixtures/RelationSpikeFixture/Sources/RelationSpike/Spike.swift
  - Tests/scip-swiftTests/RelationSpikeTests.swift
  - Sources/scip-swift/SCIPMapping/RelationshipMapping.swift
  - Tests/scip-swiftTests/RelationshipMappingTests.swift
  - Sources/scip-swift/SCIPMapping/SCIPIndexBuilder.swift
autonomous: true
requirements: [META-06, META-01, TEST-02]
estimate:
  tokens: 48000
  raw_tokens: 48000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - Indexing RelationSpikeFixture produces Scip_Relationship entries on class inheritance and method override definition occurrences (per META-01, META-06)
    - scip lint passes on the output index containing relationships — no missingSymbolInRelationshipError (per META-01)
    - RelationshipMapping unit tests verify all relation role types map to correct SCIP Relationship booleans (per TEST-02)
    - The spike diagnostic test empirically confirms Swift compiler populates occurrence.relations for inheritance, conformance, and override patterns (per META-06)
  artifacts:
    - Fixtures/RelationSpikeFixture/Package.swift
    - Fixtures/RelationSpikeFixture/Sources/RelationSpike/Spike.swift
    - Tests/scip-swiftTests/RelationSpikeTests.swift
    - Sources/scip-swift/SCIPMapping/RelationshipMapping.swift
    - Tests/scip-swiftTests/RelationshipMappingTests.swift
  key_links:
    - SCIPIndexBuilder.makeDocument calls RelationshipMapping.scipRelationships(for:symbolFormatter:) for occurrences whose roles contain .definition
    - RelationshipMapping maps .baseOf/.extendedBy/.specializationOf to isImplementation, .overrideOf to isReference+isImplementation, excludes .childOf (enclosing_symbol) and drops .calledBy/.accessorOf/.containedBy/.ibTypeOf/.receivedBy
    - Post-loop same-document filter ensures every Relationship.symbol target exists in definedSymbols before emission
---

<objective>
Tracer slice: prove the end-to-end relationship mapping path — build a Swift fixture with inheritance/override patterns, empirically validate IndexStoreDB relation population (META-06 spike), create the RelationshipMapping pure-function mapper (TEST-02), and wire it into SCIPIndexBuilder so the emitted .scip index contains Scip_Relationship entries that pass scip lint (META-01).

Purpose: This is the single biggest unknown in the roadmap. The spike determines whether Swift populates occurrence.relations with the same depth as Clang, and in which direction .baseOf flows. The RelationshipMapping + SCIPIndexBuilder wiring proves the architecture end-to-end.

Output: A new fixture package, a diagnostic spike test, a new stateless mapper with unit tests, and a modified SCIPIndexBuilder that emits relationships.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-symbol-metadata-enrichment/01-RESEARCH.md
@.planning/phases/01-symbol-metadata-enrichment/01-PATTERNS.md
@.planning/codebase/CONVENTIONS.md
@.planning/codebase/TESTING.md
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Spike fixture + diagnostic integration test validating relation population (META-06)</name>
  <files>
    Fixtures/RelationSpikeFixture/Package.swift,
    Fixtures/RelationSpikeFixture/Sources/RelationSpike/Spike.swift,
    Tests/scip-swiftTests/RelationSpikeTests.swift
  </files>
  <read_first>
    - Fixtures/MiniSwiftPackage/Package.swift — fixture package pattern (swift-tools-version: 6.2, minimal target)
    - Tests/scip-swiftTests/IntegrationTests.swift — integration test pattern (SwiftPMBuildRunner, produceIndexStore, IndexStoreLoader.open, fixture path resolution via #filePath)
    - Sources/scip-swift/Build/SwiftPMBuildRunner.swift — produceIndexStore() signature and scratchPath usage
    - Sources/scip-swift/IndexStore/IndexStoreLoader.swift — open(storePath:databasePath:) signature
    - .planning/phases/01-symbol-metadata-enrichment/01-RESEARCH.md lines 488-605 — spike fixture patterns and methodology
  </read_first>
  <behavior>
    - Test "class inheritance produces baseOf relation on derived class": Build the fixture, query symbolOccurrences for Spike.swift, find the Dog class definition occurrence, assert it has at least one relation. If a relation with .baseOf role exists, assert its symbol USR matches Animal's USR (confirms direction: derived→base).
    - Test "method override produces overrideOf relation": Find Dog.makeSound definition occurrence, assert it has a relation with .overrideOf role pointing to Animal.makeSound.
    - Test "protocol conformance produces baseOf or extendedBy relation": Find Greeter struct definition, assert it has a relation pointing to Greetable protocol.
    - Each test also prints diagnostic output (symbol name, USR, roles, all relation details) so failures are interpretable.
  </behavior>
  <action>
    Create Fixtures/RelationSpikeFixture/ as a minimal SwiftPM package mirroring MiniSwiftPackage structure. Package.swift uses swift-tools-version: 6.2, name "RelationSpikeFixture", target "RelationSpike".

    Create Spike.swift with exactly these 5 patterns from RESEARCH.md lines 500-535: (1) class Animal with instance method makeSound returning String; (2) class Dog inheriting Animal with override func makeSound; (3) protocol Greetable with func greet returning String; (4) struct Greeter conforming to Greetable with greet implementation; (5) extension Dog conforming to Greetable with greet implementation. Also include protocol Drawable with func draw, protocol Shape inheriting Drawable, and func outerFunction containing a local let localValue = 42. All types and methods must be public so they appear as definition occurrences.

    Create RelationSpikeTests.swift as a Swift Testing @Suite("META-06: Relation Spike") struct. Follow the IntegrationTests pattern: resolve fixture path via URL(fileURLWithPath: #filePath).deletingLastPathComponent() chain navigating to Fixtures/RelationSpikeFixture, create a temporary work directory with defer cleanup, use SwiftPMBuildRunner with scratchPath under the temp directory, call produceIndexStore(), open IndexStoreDB via IndexStoreLoader.open(storePath:databasePath:). Query indexStoreDB.symbolOccurrences(inFilePath:) for the absolute path to Spike.swift within the fixture.

    For each @Test, iterate occurrences sorted, locate the target symbol by matching occ.symbol.name and occ.roles.contains(.definition), then assert occ.relations is non-empty and contains the expected relation role. Print full diagnostic detail (symbol name, USR, roles, each relation's symbol name/USR/roles) before assertions so RED failures are interpretable. Use #expect for assertions and #require for unwrapping found occurrences.

    If relations are NOT populated for a pattern (test fails), the diagnostic output shows the actual state. The executor reads the output and documents findings in the task summary. If .baseOf direction is reversed (appears on Animal's occurrence pointing to Dog instead of Dog's occurrence pointing to Animal), adjust expectations accordingly and note it for Task 2's mapping table.
  </action>
  <verify>
    <automated>swift test --filter RelationSpike</automated>
  </verify>
  <acceptance_criteria>
    - Fixtures/RelationSpikeFixture/Package.swift exists with swift-tools-version: 6.2 and target "RelationSpike"
    - Fixtures/RelationSpikeFixture/Sources/RelationSpike/Spike.swift exists with all 5 patterns
    - swift test --filter RelationSpike passes (relations are populated for inheritance and override patterns)
    - If any pattern fails, diagnostic output is present in test output showing the actual relation state
    - Test output documents the .baseOf direction (whether it appears on Dog→Animal or Animal→Dog)
  </acceptance_criteria>
  <done>
    RelationSpikeFixture builds successfully and the spike test confirms IndexStoreDB populates occurrence.relations for Swift inheritance and override patterns with a determinable direction.
  </done>
</task>

<task type="tdd" tdd="true">
  <name>Task 2: RelationshipMapping pure-function mapper + unit tests (TEST-02)</name>
  <files>
    Sources/scip-swift/SCIPMapping/RelationshipMapping.swift,
    Tests/scip-swiftTests/RelationshipMappingTests.swift
  </files>
  <read_first>
    - Sources/scip-swift/SCIPMapping/SymbolKindMapping.swift — enum-as-namespace pattern, exhaustive switch, file-level doc comment
    - Sources/scip-swift/SCIPMapping/SymbolRoleMapping.swift — bit-testing with .contains() on SymbolRole OptionSet
    - Tests/scip-swiftTests/SymbolKindMappingTests.swift — makeSymbol helper pattern for constructing Symbol test fixtures
    - .planning/phases/01-symbol-metadata-enrichment/01-RESEARCH.md lines 176-240 — Relationship Role Mapping Table (authoritative source for which roles map to which SCIP booleans)
    - Task 1's spike test output (RelationSpikeTests) — confirms .baseOf direction for mapping table accuracy
  </read_first>
  <behavior>
    - Test "overrideOf maps to isReference and isImplementation": Construct SymbolRelation with roles containing .overrideOf, pass to scipRelationships, assert result has exactly one Scip_Relationship with isReference==true AND isImplementation==true.
    - Test "baseOf maps to isImplementation only": Construct SymbolRelation with .baseOf, assert result Scip_Relationship has isImplementation==true AND isReference==false.
    - Test "extendedBy maps to isImplementation only": Construct SymbolRelation with .extendedBy, assert isImplementation==true AND isReference==false.
    - Test "specializationOf maps to isImplementation": Construct SymbolRelation with .specializationOf, assert isImplementation==true.
    - Test "childOf is excluded — no Relationship emitted": Construct SymbolRelation with .childOf, assert scipRelationships returns empty array.
    - Test "calledBy is dropped": Construct SymbolRelation with .calledBy, assert empty array.
    - Test "accessorOf is dropped": Construct SymbolRelation with .accessorOf, assert empty array.
    - Test "empty relations returns empty array": Pass [], assert empty result.
    - Test "mixed childOf and baseOf produces one Relationship": Construct [SymbolRelation(.childOf), SymbolRelation(.baseOf)], assert exactly one Scip_Relationship (the .baseOf one).
    - Test "symbolFormatter is applied to relation symbol": Use a closure that prepends "fmt:" to the USR, assert rel.symbol starts with "fmt:".
  </behavior>
  <action>
    Write RelationshipMappingTests.swift FIRST (RED). Create @Suite("RelationshipMapping") struct with private helper makeRelation(symbol:roles:) constructing SymbolRelation(symbol: Symbol(usr:name:kind:language:), roles:). Use a simple symbolFormatter closure like { symbol in "scip-swift test Module . `\(symbol.usr)`." } for test assertions. Run tests to confirm RED.

    Then create RelationshipMapping.swift (GREEN). Follow the enum-as-namespace pattern: `enum RelationshipMapping { static func ... }`. Import IndexStoreDB. File-level doc comment references META-01 and explains the mapping rationale (which roles map to which booleans, why .childOf is excluded, why .calledBy etc. are dropped).

    The function signature: `static func scipRelationships(for relations: [SymbolRelation], symbolFormatter: (Symbol) -> String) -> [Scip_Relationship]`. Use compactMap to iterate relations and return nil for excluded/dropped ones. For each relation: skip if roles.contains(.childOf). Create var rel = Scip_Relationship(). Set rel.symbol = symbolFormatter(relation.symbol). If roles.contains(.baseOf) OR roles.contains(.extendedBy) OR roles.contains(.specializationOf): set rel.isImplementation = true. If roles.contains(.overrideOf): set rel.isReference = true AND rel.isImplementation = true. Guard: return nil if none of isImplementation/isReference/isTypeDefinition/isDefinition is true (per RESEARCH.md Pitfall 1 — at least one boolean must be set or scip lint errors with missingRelationshipFlagError). Return rel.

    Adjust the .baseOf mapping direction if Task 1's spike showed interpretation B (baseOf on the base class pointing to derived). In that case, the spike output would have shown .baseOf on Animal's occurrence, and the mapping must reverse-lookup or be adjusted. Per RESEARCH.md lines 586-590, if interpretation B is confirmed, use occurrences(relatedToUSR:roles:) during build post-processing instead. Document the chosen direction in the file-level doc comment.
  </action>
  <verify>
    <automated>swift test --filter RelationshipMapping</automated>
  </verify>
  <acceptance_criteria>
    - Sources/scip-swift/SCIPMapping/RelationshipMapping.swift exists as `enum RelationshipMapping` with static func scipRelationships(for:symbolFormatter:) -> [Scip_Relationship]
    - Tests/scip-swiftTests/RelationshipMappingTests.swift exists with @Suite("RelationshipMapping") and at least 8 @Test cases covering overrideOf, baseOf, extendedBy, specializationOf, childOf exclusion, calledBy drop, empty array, mixed relations
    - swift test --filter RelationshipMapping passes all cases
    - No fenced code blocks in implementation beyond what the mapper pattern requires (follow SymbolKindMapping.swift style)
  </acceptance_criteria>
  <done>
    RelationshipMapping unit tests pass, verifying the complete relation-role-to-SCIP-boolean mapping table from RESEARCH.md.
  </done>
</task>

<task type="auto">
  <name>Task 3: Wire RelationshipMapping into SCIPIndexBuilder + verify scip lint (META-01)</name>
  <files>
    Sources/scip-swift/SCIPMapping/SCIPIndexBuilder.swift
  </files>
  <read_first>
    - Sources/scip-swift/SCIPMapping/SCIPIndexBuilder.swift — current makeDocument() loop at lines 61-112, build() at lines 16-45, external_symbols heuristic at lines 40-43
    - Sources/scip-swift/SCIPMapping/RelationshipMapping.swift — the mapper created in Task 2
    - Sources/scip-swift/SCIPMapping/SCIPSymbolFormatter.swift — globalSymbolString(packageManager:moduleName:usr:) signature for formatting relation target symbols
    - .planning/phases/01-symbol-metadata-enrichment/01-RESEARCH.md lines 610-695 — Modified makeDocument() integration point and cross-document relationship handling
  </read_first>
  <action>
    Modify SCIPIndexBuilder.makeDocument() to wire in RelationshipMapping. Inside the occurrence loop, after setting symbolInformation.kind, when occurrence.roles.contains(.definition), call RelationshipMapping.scipRelationships(for: occurrence.relations, symbolFormatter:) where the closure captures buildToolName and occurrence.location.moduleName, formatting each relation symbol via SCIPSymbolFormatter.globalSymbolString(packageManager:moduleName:usr:). Store the resulting relationships on symbolInformation.relationships before adding to definedSymbols.

    After the occurrence loop and before document.symbols assignment, add a same-document filtering pass (Approach A from RESEARCH.md lines 688-689): iterate definedSymbols, for each value filter its relationships array to only those whose .symbol field exists as a key in definedSymbols. This prevents scip lint missingSymbolInRelationshipError for cross-document or external targets. Reassign the filtered values back to definedSymbols. This is necessary because relationship targets (e.g., Animal when Dog inherits from Animal) must exist in the index's symbols or external_symbols; same-document filtering guarantees this for the spike fixture where all types are in one file.

    Do NOT change the existing external_symbols heuristic in build() at this point — META-04 (isSystem classification) is Plan 3's scope. Do NOT change the SymbolRoleMapping call site — META-03 (role expansion) is Plan 2's scope. Only add relationship wiring and post-loop filtering.

    After implementation, build the project: swift build. Then run the spike fixture through the full pipeline manually to verify relationships appear: build RelationSpikeFixture with swift build --enable-index-store, run scip-swift on it, deserialize and check the output. Alternatively, extend RelationSpikeTests with a new @Test that runs SCIPIndexBuilder.build() and asserts at least one SymbolInformation in the output document has non-empty relationships. Run scip lint on the produced .scip file to verify no missingSymbolInRelationshipError.
  </action>
  <verify>
    <automated>swift build && swift test --filter RelationSpike</automated>
  </verify>
  <acceptance_criteria>
    - SCIPIndexBuilder.makeDocument() calls RelationshipMapping.scipRelationships for definition occurrences and stores results on symbolInformation.relationships
    - Post-loop same-document filter removes relationships whose targets are not in definedSymbols
    - swift build succeeds with no compiler errors
    - swift test --filter RelationSpike passes (spike tests still pass with the wiring change)
    - At least one SymbolInformation in the RelationSpikeFixture output has non-empty relationships
    - scip lint on the produced index passes without missingSymbolInRelationshipError (run: build fixture, run scip-swift binary on it, run scip lint on the .scip output)
  </acceptance_criteria>
  <done>
    The RelationSpikeFixture output .scip index contains Scip_Relationship entries on inheritance/override symbols and passes scip lint validation.
  </done>
  <reversibility rating="reversible">Relationship wiring is additive — removing the RelationshipMapping call and post-loop filter restores original behavior.</reversibility>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| compiler→IndexStoreDB | Swift compiler output is trusted (local build, not network input) |
| IndexStoreDB→SCIP mapping | Compiler-generated relation data enters mapping logic; no user-controlled input |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-01 | Tampering | IndexStoreDB relation data | low | accept | IndexStore data comes from local Swift compiler — not a network input. CLI developer tool, trusted source. |
| T-01-02 | Tampering | Symbol USRs in relationship targets | low | accept | USRs are compiler-mangled strings used verbatim in SCIP symbol formatting; SCIPSymbolFormatter escapes them per SCIP grammar. They do not influence filesystem access. |
| T-01-SC | Tampering | Package installs | N/A | accept | Phase 1 installs zero external packages — no package legitimacy risk. |
</threat_model>

<verification>
1. swift build — project compiles with RelationshipMapping integrated
2. swift test --filter RelationshipMapping — all unit tests pass
3. swift test --filter RelationSpike — spike fixture builds and relations are populated
4. scip lint on RelationSpikeFixture output — no missingSymbolInRelationshipError, no missingRelationshipFlagError
5. Manual inspection: at least one SymbolInformation has non-empty relationships array with correct isImplementation/isReference booleans
</verification>

<success_criteria>
- Indexing a file with `class B: A {}` produces a Relationship on B pointing to A with is_implementation == true
- RelationshipMapping unit tests cover all relation role types (overrideOf, baseOf, extendedBy, specializationOf, childOf exclusion, dropped roles)
- scip lint passes on the output index containing relationships
- The spike empirically confirms Swift relation population depth and direction
</success_criteria>

<output>
Create `.planning/phases/01-symbol-metadata-enrichment/01-01-SUMMARY.md` when done
</output>

<artifacts>
This plan produces:
- `Fixtures/RelationSpikeFixture/` — Swift package with inheritance/conformance/override patterns for spike validation
- `Sources/scip-swift/SCIPMapping/RelationshipMapping.swift` — new stateless mapper: SymbolRelation[] → Scip_Relationship[]
- `Tests/scip-swiftTests/RelationshipMappingTests.swift` — unit tests for the mapper
- `Tests/scip-swiftTests/RelationSpikeTests.swift` — integration test validating IndexStoreDB relation population
- Modified `SCIPIndexBuilder.swift` — occurrence loop now reads occurrence.relations and emits relationships with same-document filtering
</artifacts>
