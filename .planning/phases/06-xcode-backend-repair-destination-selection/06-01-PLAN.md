---
phase: 06-xcode-backend-repair-destination-selection
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - Sources/scip-swift/Commands/IndexCommand.swift
  - Tests/scip-swiftTests/XcodeIntegrationTests.swift
autonomous: true
requirements: [REPAIR-01]
estimate:
  tokens: 30000
  raw_tokens: 30000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - Running indexOneRepo on Fixtures/XcodeTestProject with no cache flags (temp branch) dispatches to XcodebuildBuildRunner and returns an index with documents
    - Running indexOneRepo with a cacheDir (persistent-cache branch) also dispatches to XcodebuildBuildRunner, with derived data under the cache dir
    - SwiftPM repos keep dispatching to SwiftPMBuildRunner (existing IntegrationTests and cache suites stay green)
  artifacts:
    - path: "Sources/scip-swift/Commands/IndexCommand.swift"
      provides: "Shared produceIndexStore(tool:...) helper with switch tool called from both cache branches"
    - path: "Tests/scip-swiftTests/XcodeIntegrationTests.swift"
      provides: "Two end-to-end indexOneRepo integration tests (temp branch + cache branch) against Fixtures/XcodeTestProject"
  key_links:
    - indexOneRepo both branches -> shared produceIndexStore(tool:repoPath:configuration:scheme:scratchPath:) helper -> BuildBackendDetector's detected tool drives runner choice
    - Helper .xcodebuild case -> XcodeProjectLocator.workspaceOrProjectArguments + resolveScheme -> XcodebuildBuildRunner with derivedDataPath beside the scratch dir
---

<objective>
Restore the lost `.xcodebuild` dispatch in `IndexCommand.indexOneRepo` so Xcode-project repos index again (REPAIR-01). The dispatch branch was dropped in commit `0cdefd7` (phase-3 cache rewrite); both runner-construction sites now unconditionally build `SwiftPMBuildRunner`, so `.xcodeproj`/`.xcworkspace` repos get a misleading `swift build` failure.

Purpose: Without this, the xcodebuild backend is unreachable and every Phase 6 destination feature builds on a dead code path.
Output: One shared dispatch helper called from both cache branches, plus two real-xcodebuild integration tests proving both branches work end-to-end.
</objective>

<execution_context>
@$HOME/.codex/gsd-core/workflows/execute-plan.md
@$HOME/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-xcode-backend-repair-destination-selection/06-RESEARCH.md
@.planning/phases/06-xcode-backend-repair-destination-selection/06-VALIDATION.md

@Sources/scip-swift/Commands/IndexCommand.swift
@Sources/scip-swift/Build/XcodebuildBuildRunner.swift
@Sources/scip-swift/Build/XcodeProjectLocator.swift
@Sources/scip-swift/Build/SwiftPMBuildRunner.swift
@Tests/scip-swiftTests/XcodeIntegrationTests.swift
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Restore xcodebuild dispatch on the default (temp-branch) path — end-to-end</name>
  <files>
    Sources/scip-swift/Commands/IndexCommand.swift,
    Tests/scip-swiftTests/XcodeIntegrationTests.swift
  </files>
  <read_first>
    - Sources/scip-swift/Commands/IndexCommand.swift — the two SwiftPMBuildRunner construction sites (temp branch ~line 98, persistent branch ~line 87) that lost the switch
    - Sources/scip-swift/Build/XcodebuildBuildRunner.swift — runner init fields (repoPath, configuration, scheme, derivedDataPath, projectArguments)
    - Sources/scip-swift/Build/XcodeProjectLocator.swift — workspaceOrProjectArguments and resolveScheme signatures
    - .planning/phases/06-xcode-backend-repair-destination-selection/06-RESEARCH.md §1 — verbatim lost code recovered from commit 1c5ba8f
    - Tests/scip-swiftTests/XcodeIntegrationTests.swift — existing fullPipeline test and fixtureRepoPath/makeTemporaryDirectory helpers to reuse
  </read_first>
  <behavior>
    - New integration test "indexOneRepo builds an Xcode fixture through the xcodebuild backend": calls IndexCommand.indexOneRepo(repoPath: fixtureRepoPath(), output: nil, buildTool: nil, configuration: .debug, scheme: nil, cacheDir: nil, indexOnly: false, symbolVersion: "") and expects index.documents.count > 0 with a Swift first document
    - The test fails RED before the fix (SwiftPMBuildRunner runs `swift build` against a directory with no Package.swift)
    - After the fix, the same call succeeds through XcodebuildBuildRunner
    - The existing fullPipeline test stays green (no behavior change for direct runner use)
  </behavior>
  <action>
    RED first: add the integration test above to XcodeIntegrationTests (Swift Testing @Test, reusing the suite's existing fixtureRepoPath helper; clean up any temp dirs via defer). Run it, confirm it fails with a SwiftPM build error, commit as test(phase-6).

    GREEN: in IndexCommand.swift extract a private static helper produceIndexStore(tool:repoPath:configuration:scheme:scratchPath:) that returns the produced indexStorePath as String and switches on tool. The .swiftpm case moves the existing SwiftPMBuildRunner(repoPath:configuration:scratchPath:) construction verbatim. The .xcodebuild case restores the code lost in 0cdefd7 (recover verbatim from commit 1c5ba8f via git show): XcodeProjectLocator.workspaceOrProjectArguments(repoPath:), then XcodeProjectLocator.resolveScheme(explicitScheme: scheme, projectArguments:projectArguments:repoPath:), then construct XcodebuildBuildRunner with derivedDataPath set to "derived-data" beside the scratch directory (scratchPath's parent directory) so temp runs put derived data under the temp work directory, matching the original workDirectory/derived-data convention. Rewire ONLY the no-cache else-branch to call the helper this task; the persistent-cache branch is Task 2. Keep 2-space indent, enum-namespace conventions, no comments. Do NOT touch the --index-only path — its SwiftPMBuildRunner.findIndexStore asymmetry is explicitly out of scope this phase. Run the new test green, commit as feat(phase-6).
  </action>
  <verify>
    <automated>swift test --filter XcodeIntegrationTests</automated>
  </verify>
  <done>
    indexOneRepo with no cache flags on Fixtures/XcodeTestProject returns an index with documents via the xcodebuild backend; the tracer test is RED before the helper exists and GREEN after; committed in two commits (test, then feat).
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Wire the persistent-cache branch through the shared dispatch helper</name>
  <files>
    Sources/scip-swift/Commands/IndexCommand.swift,
    Tests/scip-swiftTests/XcodeIntegrationTests.swift
  </files>
  <read_first>
    - Sources/scip-swift/Commands/IndexCommand.swift — persistentCache branch (cacheDir != nil || indexOnly) still inlining SwiftPMBuildRunner after Task 1
    - Tests/scip-swiftTests/XcodeIntegrationTests.swift — Task 1's test as the pattern for this one
  </read_first>
  <behavior>
    - New integration test "indexOneRepo with --cache-dir also builds an Xcode fixture through xcodebuild": calls indexOneRepo with cacheDir set to a fresh temp directory (indexOnly: false) and expects index.documents.count > 0
    - The test additionally asserts the derived-data directory landed under the cache dir (cacheDir/derived-data exists), pinning the cache-branch layout convention
    - The test fails RED while the persistent branch still constructs SwiftPMBuildRunner
  </behavior>
  <action>
    RED first: add the cache-branch integration test (fresh temp cacheDir per run via the suite's makeTemporaryDirectory helper, defer cleanup — the cache manifest and index-db the pipeline writes into it are disposable). Run it, confirm RED, commit as test(phase-6).

    GREEN: replace the persistent-cache branch's inline SwiftPMBuildRunner construction with a call to the shared produceIndexStore(tool:repoPath:configuration:scheme:scratchPath:) helper from Task 1. Both branches now share one dispatch point, so the bug cannot survive in either path. Then run the whole Xcode suite plus the SwiftPM regression guards: existing IntegrationTests, and the phase-3 cache suites if any exercise indexOneRepo. Commit as feat(phase-6).
  </action>
  <verify>
    <automated>swift test --filter Xcode && swift test --filter IntegrationTests</automated>
  </verify>
  <done>
    indexOneRepo with a cacheDir dispatches Xcode fixtures to XcodebuildBuildRunner and puts derived data under the cache dir; both indexOneRepo branches call one shared switch-tool helper; SwiftPM integration tests stay green.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CLI user → subprocess | Repo path and (later) destination strings cross from user input into xcodebuild/swift build invocation |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-06-01 | Tampering | indexOneRepo → BuildRunner dispatch | low | accept | Repo path is user-local (they can already run builds directly); restored code passes paths through Process argument arrays via existing SubprocessRunner — no shell string building introduced |
| T-06-02 | Information disclosure | BuildError.buildFailed full output | low | accept | Untruncated build output matches the existing convention; the output is the user's own local build log |
</threat_model>

<verification>
- swift test --filter XcodeIntegrationTests — both new indexOneRepo integration tests green (real xcodebuild against the fixture, ~60s each on cold DerivedData)
- swift test --filter IntegrationTests — SwiftPM path unregressed
- swift build — IndexManyCommand and all indexOneRepo call sites compile unchanged (helper is additive)
</verification>

<success_criteria>
- A user can run `scip-swift index <xcode-repo>` (with or without `--cache-dir`) and get a `.scip` index built through the xcodebuild backend (REPAIR-01)
- Exactly one switch-tool dispatch helper exists, called from both cache branches of indexOneRepo
- No SwiftPM behavior change: existing IntegrationTests and cache suites green
- Full `swift test` green before phase verify
</success_criteria>

<output>
Create `.planning/phases/06-xcode-backend-repair-destination-selection/06-01-SUMMARY.md` when done
</output>
