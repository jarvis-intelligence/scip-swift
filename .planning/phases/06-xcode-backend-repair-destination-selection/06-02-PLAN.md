---
phase: 06-xcode-backend-repair-destination-selection
plan: 02
type: tdd
wave: 2
depends_on: ["06-01"]
files_modified:
  - Sources/scip-swift/Build/XcodebuildBuildRunner.swift
  - Sources/scip-swift/Build/BuildError.swift
  - Sources/scip-swift/Commands/IndexCommand.swift
  - Tests/scip-swiftTests/XcodebuildBuildRunnerTests.swift
  - Tests/scip-swiftTests/XcodeIntegrationTests.swift
autonomous: true
requirements: [REPAIR-02, REPAIR-03]
estimate:
  tokens: 35000
  raw_tokens: 35000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - Passing --destination "platform=iOS Simulator,name=iPhone 16" makes xcodebuild receive ["-destination", spec] spliced after -configuration and before -derivedDataPath
    - Omitting --destination keeps the xcodebuild argument list byte-identical to today (existing 7 XcodebuildBuildRunnerTests are the regression guard and stay green)
    - A --destination build that exits non-zero throws BuildError.xcodebuildDestinationFailed whose description contains the full untruncated xcodebuild output and a hint line ending in -showdestinations
    - A nil-destination build failure still throws the existing .buildFailed, unchanged
    - scip-swift index --build-tool xcodebuild --destination <spec> reaches the runner from the CLI surface
  artifacts:
    - path: "Sources/scip-swift/Build/XcodebuildBuildRunner.swift"
      provides: "destination: String? = nil stored property; -destination splice in the pure arguments property; destination-gated failure throw in produceIndexStore"
    - path: "Sources/scip-swift/Build/BuildError.swift"
      provides: "New xcodebuildDestinationFailed case with full-output description plus -showdestinations hint"
    - path: "Sources/scip-swift/Commands/IndexCommand.swift"
      provides: "@Option --destination on IndexCommand threaded through indexOneRepo as a defaulted parameter"
  key_links:
    - IndexCommand @Option destination -> indexOneRepo(destination:) defaulted param -> shared dispatch helper from 06-01 -> XcodebuildBuildRunner(destination:) -> arguments splice
    - XcodebuildBuildRunner.produceIndexStore() non-zero exit + destination != nil -> BuildError.xcodebuildDestinationFailed (not .buildFailed)
---

<objective>
Add opt-in `--destination <spec>` selection for the xcodebuild backend (REPAIR-02) and a destination-specific failure hint (REPAIR-03). No BuildRunner protocol change: destination is xcodebuild-specific config carried on the runner struct and spliced into the pure `arguments` property.

Purpose: iOS-only targets do not fully index under xcodebuild's default generic destination; users need to point the build at an explicit simulator/device, and need discoverable help when the spec is wrong.
Output: Runner destination support with unit-guarded arg construction, new BuildError case, CLI threading, unit + integration tests.
</objective>

<execution_context>
@$HOME/.codex/gsd-core/workflows/execute-plan.md
@$HOME/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-xcode-backend-repair-destination-selection/06-RESEARCH.md
@.planning/phases/06-xcode-backend-repair-destination-selection/06-VALIDATION.md
@.planning/phases/06-xcode-backend-repair-destination-selection/06-01-SUMMARY.md

@Sources/scip-swift/Build/XcodebuildBuildRunner.swift
@Sources/scip-swift/Build/BuildError.swift
@Sources/scip-swift/Commands/IndexCommand.swift
@Sources/scip-swift/Commands/IndexManyCommand.swift
@Tests/scip-swiftTests/XcodebuildBuildRunnerTests.swift
@Tests/scip-swiftTests/XcodeIntegrationTests.swift
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: destination property + argument splice (pure unit, REPAIR-02)</name>
  <files>
    Sources/scip-swift/Build/XcodebuildBuildRunner.swift,
    Tests/scip-swiftTests/XcodebuildBuildRunnerTests.swift
  </files>
  <read_first>
    - Sources/scip-swift/Build/XcodebuildBuildRunner.swift — the arguments property and the comment block at lines 24-31 that currently justifies no-destination defaults
    - Tests/scip-swiftTests/XcodebuildBuildRunnerTests.swift — the 7 existing assertions and the value(after:in:) helper; note makeRunner does not pass destination today
  </read_first>
  <behavior>
    - Test "nil destination keeps the argument list identical to the no-flag form": runner built with destination omitted (default) has arguments equal to a runner built with destination: nil — and contains zero occurrences of "-destination"
    - Test "non-nil destination splices after -configuration and before -derivedDataPath": args contains "-destination" followed immediately by the verbatim spec; index(after: "-configuration") < destinationIndex < index(of: "-derivedDataPath")
    - Test "destination precedes the build action": the "-destination" token index < "build" token index
    - The existing 7 tests stay green with zero edits (their makeRunner helper compiles unchanged)
  </behavior>
  <action>
    RED first: extend XcodebuildBuildRunnerTests with the three tests above. Extend makeRunner with a defaulted destination: String? = nil parameter and pass it through. Run — the destination tests fail RED (property does not exist → compile failure counts as RED for this plan; write the tests first, then confirm the compile error mentions destination, then proceed). Commit as test(phase-6).

    GREEN: in XcodebuildBuildRunner add `var destination: String? = nil` as the last stored property — declare it as var-with-default, NOT let-optional: a live compile check this session proved a bare let-optional requires every call site to pass the parameter, while the var-default form synthesizes a memberwise init where the parameter is omittable, keeping all existing call sites (unit helpers, 06-01 dispatch helper, integration tests) compiling without edits. In arguments, build the list as today then splice ["-destination", destination] immediately after "-configuration <name>" when destination is non-nil, strictly before "-derivedDataPath" and all CODE_SIGN settings and "build". Nil must leave the list unchanged element-for-element. Rewrite the lines 24-31 comment block so it reflects the new policy: no destination by default; --destination opts in, and signing stays disabled because an index build never ships a product — keep the reasoning about GatherProvisioningInputs failure, reworded for the opt-in world. Do not reorder or drop the existing CODE_SIGN settings. Commit as feat(phase-6).
  </action>
  <verify>
    <automated>swift test --filter XcodebuildBuildRunnerTests</automated>
  </verify>
  <done>
    The 7 pre-existing assertions pass unchanged plus 3 new destination assertions green; nil destination produces a byte-identical argument list; non-nil splices -destination <spec> after -configuration, before -derivedDataPath and build.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: xcodebuildDestinationFailed error case (REPAIR-03)</name>
  <files>
    Sources/scip-swift/Build/BuildError.swift,
    Sources/scip-swift/Build/XcodebuildBuildRunner.swift,
    Tests/scip-swiftTests/XcodebuildBuildRunnerTests.swift
  </files>
  <read_first>
    - Sources/scip-swift/Build/BuildError.swift — the .buildFailed case and its full-output description style (lines 15-19, 43-47); the exhaustive switch in description
    - Sources/scip-swift/Build/XcodebuildBuildRunner.swift — the produceIndexStore() guard on result.exitCode currently throwing .buildFailed
    - 06-RESEARCH.md §3 — live-captured failure marker "xcodebuild: error: Unable to find a device matching the provided destination specifier:" and verified -showdestinations behavior
  </read_first>
  <behavior>
    - Test "destination failure description carries full output and the hint": BuildError.xcodebuildDestinationFailed(exitCode: 65, output: sample) .description contains the sample output verbatim (untruncated), contains "-showdestinations", and contains the project/scheme context needed to make the hint copyable
    - Test "hint fires only for destination builds": the throw-site condition is destination != nil AND exitCode != 0 — assert via the pure seam where possible; the nil path keeps throwing .buildFailed (covered by the integration bogus-failure staying untouched)
  </behavior>
  <action>
    RED first: add the description tests to XcodebuildBuildRunnerTests (they target BuildError; assertable without spawning xcodebuild — keep them in this suite so the whole gate runs under the sub-5s filter). Run RED, commit test(phase-6).

    GREEN: add case xcodebuildDestinationFailed(exitCode: Int32, output: String, hintCommand: String) to BuildError with a doc comment matching the file's existing case-documentation style, and extend the description switch (it is exhaustive — the compiler forces the new arm). The description embeds the full output after the exit-code line, mirroring .buildFailed's format, then a final line presenting hintCommand as the copyable remedy. In XcodebuildBuildRunner.produceIndexStore(), change the non-zero-exit guard to check destination first: when destination != nil throw xcodebuildDestinationFailed with the runner's own projectArguments + scheme + "-showdestinations" composed as the hint command; otherwise throw .buildFailed exactly as today. Gate strictly on destination != nil — do NOT string-match xcodebuild error text (marker strings vary across Xcode versions; matching them is brittle). Commit feat(phase-6).
  </action>
  <verify>
    <automated>swift test --filter XcodebuildBuildRunnerTests</automated>
  </verify>
  <done>
    New BuildError case exists, documented, and wired at the throw site with strict destination-gating; description tests green; nil-destination failure path byte-for-byte the .buildFailed behavior it was.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: CLI threading + live destination runs (REPAIR-02 end-to-end, REPAIR-03 live)</name>
  <files>
    Sources/scip-swift/Commands/IndexCommand.swift,
    Tests/scip-swiftTests/XcodeIntegrationTests.swift
  </files>
  <read_first>
    - Sources/scip-swift/Commands/IndexCommand.swift — the @Option block (scheme at lines 25-27), the run() call site (line 36), indexOneRepo signature, and the 06-01 dispatch helper
    - Sources/scip-swift/Commands/IndexManyCommand.swift — the positional indexOneRepo call site (lines 42-50) that must compile unchanged via the defaulted parameter
    - Tests/scip-swiftTests/XcodeIntegrationTests.swift — the two 06-01 indexOneRepo tests as the pattern
  </read_first>
  <behavior>
    - Integration test "explicit destination builds the fixture and produces an index": indexOneRepo(repoPath: fixture, ..., destination: "platform=macOS", cacheDir: temp, indexOnly: false) returns index.documents.count > 0 (verified live in research: platform=macOS + the existing CODE_SIGNING_ALLOWED=NO overrides build the fixture and still produce Index.noindex/DataStore)
    - Integration test "bogus destination fails with the discoverable hint": indexOneRepo(..., destination: "platform=iOS Simulator,name=Nonexistent Device 999") throws, and the thrown error's description contains "-showdestinations"; expect the live marker "Unable to find a device matching the provided destination specifier:" in the output (run ~60s — device-matching timeout; keep it in this suite)
    - CLI surface: scip-swift index --build-tool xcodebuild --destination spec parses (verify via the binary's help text containing --destination, plus the parse reaching the runner asserted by the macOS integration test)
  </behavior>
  <action>
    RED first: add the two integration tests to XcodeIntegrationTests, each calling indexOneRepo with the destination parameter (the parameter does not exist yet — the compile failure is the RED state; write the tests, confirm, commit test(phase-6)).

    GREEN: add @Option(name: .long, help:) var destination: String? to IndexCommand beside scheme, with help text recommending "generic/platform=iOS Simulator" for index builds (it needs no device attached). Thread it: add a destination: String? = nil parameter to indexOneRepo (after scheme) and pass it through run()'s call and into the 06-01 dispatch helper, which forwards it to XcodebuildBuildRunner's init. IndexManyCommand's existing positional call site compiles unchanged via the defaulted parameter — make no edits there (per-repo destinations are deferred scope). Then run the full Xcode filter (unit + integration) and IntegrationTests for the SwiftPM path. Commit feat(phase-6).
  </action>
  <verify>
    <automated>swift test --filter Xcode && swift test --filter IntegrationTests</automated>
  </verify>
  <done>
    --destination parses on IndexCommand, threads through indexOneRepo and the dispatch helper into the runner; platform=macOS destination run produces an index; bogus destination throws xcodebuildDestinationFailed whose description contains -showdestinations; IndexManyCommand compiles untouched; full Xcode + SwiftPM suites green.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CLI user → subprocess | --destination is user-supplied free text passed to xcodebuild |
| CLI user → error output | Failed-build output (possibly containing user repo paths) rendered into terminal |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-06-03 | Tampering | destination string → xcodebuild argv | medium | mitigate | Pass verbatim as a single Process argument via existing SubprocessRunner.run argument array; never compose a shell string; the destination value is a distinct argv element so embedded spaces/semicolons cannot inject additional xcodebuild options |
| T-06-04 | Repudiation | destination-gated failure hint | low | accept | Local CLI with no audit surface; the error embeds the failing exit code and full output for diagnosis |
</threat_model>

<verification>
- swift test --filter XcodebuildBuildRunnerTests — pure arg-construction + error-description gate, sub-5s
- swift test --filter XcodeIntegrationTests — real xcodebuild: 06-01 dispatch tests, macOS destination success, bogus-destination failure with hint (~60s each)
- swift test --filter IntegrationTests — SwiftPM path unregressed
- Full swift test green before phase verify (phase gate per 06-VALIDATION.md)
</verification>

<success_criteria>
- User can pass --destination "platform=iOS Simulator,name=iPhone 16" and xcodebuild builds for that destination; omitting the flag preserves today's generic-destination behavior (REPAIR-02)
- Failed --destination builds surface full output plus a -showdestinations hint; failures without the flag keep the existing .buildFailed error (REPAIR-03)
- No BuildRunner protocol change; existing 7 runner unit tests green without edits; IndexManyCommand compiles unchanged
- Full swift test green
</success_criteria>

<output>
Create `.planning/phases/06-xcode-backend-repair-destination-selection/06-02-SUMMARY.md` when done
</output>
