---
phase: 04-cross-repo-indexing
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - Sources/scip-swift/SCIPMapping/SCIPSymbolFormatter.swift
  - Sources/scip-swift/SCIPMapping/SCIPIndexBuilder.swift
  - Sources/scip-swift/Commands/IndexCommand.swift
  - Sources/scip-swift/Commands/IndexManyCommand.swift
  - Sources/scip-swift/SCIPMapping/ScipIndexMerger.swift
  - Sources/scip-swift/ScipSwiftCommand.swift
  - Tests/scip-swiftTests/SCIPSymbolFormatterTests.swift
  - Tests/scip-swiftTests/ScipIndexMergerTests.swift
autonomous: true
requirements:
  - CROSS-01
  - CROSS-02
  - CROSS-03
estimate:
  tokens: 30000
  raw_tokens: 30000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - SCIPSymbolFormatter.globalSymbolString populates the version field segment when a non-empty version string is supplied
    - Existing symbol format is unchanged when version defaults to empty (backward compatibility with all existing .scip indexes)
    - scip-swift index-many accepts two or more repo paths as variadic arguments
    - scip-swift index-many indexes each repo independently via the extracted indexOneRepo function
    - scip-swift index-many --merge combines multiple Scip_Index objects into a single merged output via ScipIndexMerger
    - The existing scip-swift index subcommand behavior is unchanged
  artifacts:
    - Sources/scip-swift/SCIPMapping/SCIPSymbolFormatter.swift
    - Sources/scip-swift/SCIPMapping/SCIPIndexBuilder.swift
    - Sources/scip-swift/Commands/IndexCommand.swift
    - Sources/scip-swift/Commands/IndexManyCommand.swift
    - Sources/scip-swift/SCIPMapping/ScipIndexMerger.swift
    - Sources/scip-swift/ScipSwiftCommand.swift
    - Tests/scip-swiftTests/SCIPSymbolFormatterTests.swift
    - Tests/scip-swiftTests/ScipIndexMergerTests.swift
  key_links:
    - IndexManyCommand.run() calls IndexCommand.indexOneRepo() per repo path
    - IndexManyCommand.run() calls ScipIndexMerger.merge() when --merge flag is set
    - ScipSwiftCommand.subcommands includes IndexManyCommand.self alongside IndexCommand.self
    - SCIPIndexBuilder passes symbolVersion through to all globalSymbolString call sites
---

<objective>
Wire one end-to-end path through every layer Phase 4 touches: version field in SCIPSymbolFormatter, version threading through SCIPIndexBuilder, extracted indexOneRepo in IndexCommand, new IndexManyCommand subcommand, new ScipIndexMerger, and subcommand registration in ScipSwiftCommand.

Purpose: Prove the cross-repo architecture end-to-end before expanding merge correctness rules in Plan 02. CROSS-03 (version field) is the foundation that all symbol strings depend on; CROSS-01/CROSS-02 (index-many) build on it; the basic merge path validates the ScipIndexMerger contract.

Output: A working `scip-swift index-many <repoA> <repoB> [--merge]` command that indexes each repo independently and optionally merges results, with version disambiguation active. Existing `scip-swift index` behavior unchanged.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-cross-repo-indexing/04-RESEARCH.md

@Sources/scip-swift/SCIPMapping/SCIPSymbolFormatter.swift
@Sources/scip-swift/SCIPMapping/SCIPIndexBuilder.swift
@Sources/scip-swift/Commands/IndexCommand.swift
@Sources/scip-swift/ScipSwiftCommand.swift
@Sources/scip-swift/Build/BuildError.swift
@Tests/scip-swiftTests/SCIPSymbolFormatterTests.swift
@Tests/scip-swiftTests/IntegrationTests.swift
</context>

<tasks>

<task type="tracer">
  <name>Tracer: version field + indexOneRepo extraction + IndexManyCommand + basic ScipIndexMerger wired end-to-end</name>
  <files>Sources/scip-swift/SCIPMapping/SCIPSymbolFormatter.swift, Sources/scip-swift/SCIPMapping/SCIPIndexBuilder.swift, Sources/scip-swift/Commands/IndexCommand.swift, Sources/scip-swift/Commands/IndexManyCommand.swift, Sources/scip-swift/SCIPMapping/ScipIndexMerger.swift, Sources/scip-swift/ScipSwiftCommand.swift</files>
  <read_first>
    - Sources/scip-swift/SCIPMapping/SCIPSymbolFormatter.swift (lines 23-29: globalSymbolString signature and hardcoded escapeSpaceField("") at line 26)
    - Sources/scip-swift/SCIPMapping/SCIPIndexBuilder.swift (lines 8-26: init; lines 132-136, 160-166, 172-178: all three globalSymbolString call sites that need version parameter)
    - Sources/scip-swift/Commands/IndexCommand.swift (lines 28-128: entire run() body to extract into indexOneRepo)
    - Sources/scip-swift/ScipSwiftCommand.swift (line 9: subcommands array to add IndexManyCommand)
    - Sources/scip-swift/Build/BuildError.swift (exhaustive error enum pattern for any new error cases)
  </read_first>
  <action>
    Wire ONE path through every layer Phase 4 touches. Make six coordinated changes:

    1. SCIPSymbolFormatter.globalSymbolString: Add a `version: String = ""` parameter between `moduleName` and `usr`. Replace the hardcoded `escapeSpaceField("")` on the version line with `escapeSpaceField(version)`. The default empty string preserves the existing dot-placeholder behavior so all current symbol strings are unchanged.

    2. SCIPIndexBuilder: Add a `symbolVersion: String` stored property with default value `""` in the init (after `converterVersion`, before `cacheStore`). Pass `version: symbolVersion` to all three `SCIPSymbolFormatter.globalSymbolString` call sites in makeDocument: the main symbol string (around line 134), the enclosing symbol (around line 162), and the relationship formatter closure (around line 173).

    3. IndexCommand: Extract the entire body of run() (lines 28-128, from path resolution through index write) into a new `static func indexOneRepo(repoPath:output:buildTool:configuration:scheme:cacheDir:indexOnly:symbolVersion:) throws -> Scip_Index` on IndexCommand. The function performs the same build, cache, and index logic but returns the Scip_Index instead of writing it to disk. The writing of serializedData to the output path stays in run() — run() calls indexOneRepo then writes the result. Add `symbolVersion: String = ""` as the last parameter so IndexCommand passes empty string (backward compat). The indexstoreDbRevision constant stays on IndexCommand and is used inside indexOneRepo.

    4. IndexManyCommand: Create a new ParsableCommand struct in Commands/IndexManyCommand.swift following the existing IndexCommand pattern. Configuration: commandName "index-many", abstract "Index multiple Swift repos independently, optionally merging results." Declare variadic `@Argument` `repoPaths: [String]` with help text "Paths to Swift repos to index (two or more)." Declare `@Flag(name: .long)` `merge: Bool` default false with help "Merge all indexes into a single .scip output." Declare `@Option(name: .long)` `outputDir: String?` for individual file output directory. Declare `@Option(name: .long)` `mergedOutput: String?` for merged output path (used with --merge). Declare `@Option(name: .long)` `configuration: BuildConfiguration` default .debug. Declare `@Option(name: .long)` `cacheDir: String?`. In run(): guard repoPaths.count >= 2, else throw ValidationError. For each repo path: derive symbolVersion from `URL(fileURLWithPath: repoPath).lastPathComponent` (the directory basename — always available, sufficient for disambiguation). Call `IndexCommand.indexOneRepo(repoPath:output:nil buildTool:nil configuration:configuration scheme:nil cacheDir:cacheDir indexOnly:false symbolVersion:derivedVersion)` per repo. Collect results into an array of Scip_Index. If merge is true: call `ScipIndexMerger.merge(indexes, projectRoot: FileManager.default.currentDirectoryPath)`, then write serializedData to mergedOutput or default "merged.scip" in current directory. If merge is false: write each Scip_Index serializedData to `<outputDir or ".">/<repoBasename>.scip`. Print summary of documents written.

    5. ScipIndexMerger: Create a new file Sources/scip-swift/SCIPMapping/ScipIndexMerger.swift. Follow the project convention for stateless logic: declare `enum ScipIndexMerger` (not struct) with a single `static func merge(_ indexes: [Scip_Index], projectRoot: String) -> Scip_Index`. The merge logic: create a new Scip_Index, set metadata from the first index's metadata but override projectRoot, concatenate all documents from all indexes, build a defined-symbol Set from all document symbols, then merge externalSymbols by iterating all indexes and adding each external symbol only if it is not in the defined set and not already seen (deduplication via a dictionary keyed by symbol string). Sort externalSymbols by symbol string. Return the merged index. This basic merge is expanded with full correctness rules in Plan 02.

    6. ScipSwiftCommand: Add `IndexManyCommand.self` to the subcommands array alongside `IndexCommand.self`. IndexCommand remains the defaultSubcommand.
  </action>
  <verify>
    <automated>swift build 2>&1 | tail -5 && swift test 2>&1 | tail -10</automated>
  </verify>
  <done>
    - swift build succeeds with zero errors
    - swift test passes with zero regressions (all existing tests green)
    - SCIPSymbolFormatter.globalSymbolString accepts a version parameter with default ""
    - SCIPIndexBuilder has a symbolVersion property threaded to all globalSymbolString call sites
    - IndexCommand.indexOneRepo static function exists and IndexCommand.run() delegates to it
    - IndexManyCommand is registered as a subcommand and accepts variadic repo paths
    - ScipIndexMerger.merge produces a valid Scip_Index from multiple inputs
  </done>
</task>

<task type="auto" tdd="true">
  <name>Unit tests for version field behavior and ScipIndexMerger basics</name>
  <files>Tests/scip-swiftTests/SCIPSymbolFormatterTests.swift, Tests/scip-swiftTests/ScipIndexMergerTests.swift</files>
  <read_first>
    - Tests/scip-swiftTests/SCIPSymbolFormatterTests.swift (existing test patterns: realUSRIsEscaped, identifierOnlyStringIsUnescaped, emptyFieldPlaceholder)
    - Tests/scip-swiftTests/IntegrationTests.swift (test structure: @Suite, @Test with string descriptions, #expect, @testable import scip_swift)
    - Sources/scip-swift/SCIPMapping/ScipIndexMerger.swift (created by tracer task — the merge API to test)
  </read_first>
  <behavior>
    Version field tests (add to existing SCIPSymbolFormatterTests suite):
    - Test: globalSymbolString with version "abc123" produces a symbol string where the fourth space-delimited field is "abc123" (not the dot placeholder)
    - Test: globalSymbolString with default empty version still produces the dot placeholder in the version position (backward compatibility — existing test realUSRIsEscaped continues to pass unchanged)
    - Test: globalSymbolString with version containing spaces doubles the spaces per escapeSpaceField rules
    ScipIndexMerger tests (new ScipIndexMergerTests suite):
    - Test: merging two indexes concatenates documents from both
    - Test: merging deduplicates external symbols that appear in both indexes (same symbol string appears once)
    - Test: merged metadata uses the supplied projectRoot, not the first index's projectRoot
    - Test: merging zero indexes returns an empty Scip_Index without crashing
  </behavior>
  <action>
    Add three new tests to the existing SCIPSymbolFormatterTests suite for version field behavior: one verifying a non-empty version populates the fourth field, one verifying the default empty version preserves the dot placeholder, one verifying spaces in the version are doubled per escape rules. Do NOT modify existing tests — they already validate the default-empty behavior and should pass unchanged.

    Create a new ScipIndexMergerTests.swift file with a ScipIndexMergerTests suite containing four tests. Construct test Scip_Index instances manually using the generated protobuf types (var documents: [Scip_Document], var externalSymbols: [Scip_SymbolInformation]) — no mocks, no build steps. For the dedup test, create two indexes each with the same external symbol string and verify the merged result contains it only once. For the metadata test, verify merged.metadata.projectRoot equals the supplied projectRoot argument. For the empty-input test, call merge([], projectRoot: "/test") and verify it returns a Scip_Index with empty documents and externalSymbols arrays.
  </action>
  <verify>
    <automated>swift test --filter SCIPSymbolFormatter 2>&1 | tail -5 && swift test --filter ScipIndexMergerTests 2>&1 | tail -5</automated>
  </verify>
  <done>
    - All SCIPSymbolFormatter tests pass including 3 new version-field tests
    - All ScipIndexMergerTests pass (4 tests: concatenation, dedup, metadata projectRoot, empty input)
    - Existing tests in SCIPSymbolFormatterTests remain unchanged and green
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CLI args to index-many | User-supplied repo paths enter the filesystem operations layer |
| indexOneRepo output to ScipIndexMerger | Trusted internal data — indexes produced by our own pipeline, no external untrusted input |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-01 | Tampering | IndexManyCommand repoPaths argument | low | mitigate | URL(fileURLWithPath:).standardizedFileURL normalizes paths before filesystem access, same as existing IndexCommand pattern |
| T-04-02 | Denial of Service | ScipIndexMerger.merge processing large indexes | low | accept | Merge operates on trusted local indexes produced by our own pipeline; SwiftProtobuf has built-in size limits; no untrusted external input crosses this boundary |
| T-04-03 | Tampering | Package legitimacy (npm/pip/cargo installs) | low | accept | Phase 4 introduces zero new package dependencies — all work uses existing pinned Package.swift dependencies |
</threat_model>

<verification>
- swift build succeeds with zero compiler errors
- swift test passes with zero regressions (all pre-Phase-4 tests green)
- New unit tests for version field and ScipIndexMerger pass
- `scip-swift index-many --help` shows the subcommand with correct argument descriptions (manual check or integration in Plan 02)
</verification>

<success_criteria>
- The version field in SCIP symbol strings is populated when a non-empty version is supplied (CROSS-03)
- Default empty version preserves backward compatibility with all existing indexes (CROSS-03)
- `scip-swift index-many <repoA> <repoB>` is accepted as a valid subcommand (CROSS-01)
- Each repo is indexed independently via the extracted indexOneRepo function (CROSS-02)
- `--merge` flag produces a single combined Scip_Index via ScipIndexMerger (CROSS-04 basic path)
- No new package dependencies introduced
</success_criteria>

<output>
Create `.planning/phases/04-cross-repo-indexing/04-01-SUMMARY.md` when done
</output>
