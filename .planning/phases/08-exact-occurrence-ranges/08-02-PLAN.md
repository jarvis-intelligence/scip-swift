---
phase: 08-exact-occurrence-ranges
plan: 02
type: tdd
wave: 2
depends_on: ["08-01"]
files_modified:
  - Fixtures/UnicodeRangeFixture/Package.swift
  - Fixtures/UnicodeRangeFixture/Sources/UnicodeRange/main.swift
  - Sources/scip-swift/SCIPMapping/SCIPIndexBuilder.swift
  - Tests/scip-swiftTests/IntegrationTests.swift
  - Tests/scip-swiftTests/IncrementalIntegrationTests.swift
autonomous: true
requirements: [RANGE-01, RANGE-02, RANGE-03]
estimate:
  tokens: 28000
  raw_tokens: 28000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - The Unicode fixture .scip asserts every F4 expected 0-based byte range — emoji def L0 [4,9), getter:emoji L0 [4,9) where approximation gives 16, 名前 def L1 [4,10), getter:名前 L1 [4,10) where approximation gives 17, greet ref L1 [13,18), greet def L3 [5,10), name param L3 [11,15), String refs L3 [17,23) and [28,35), init(stringInterpolation:) L4 [2,3), name ref L4 [12,16) (RANGE-02, research F4/D6)
    - The Unicode fixture also proves RANGE-01 on the same run — the getter:名前 occurrence's exact end (10) differs from what the display-name approximation would emit (17): assert exactness, not equality with the old output
    - A source file with syntax errors still indexes through the full pipeline — the document appears in the .scip with occurrences, and any anchor that missed the token map carries the name-length approximate end (RANGE-03, research F5)
    - A cache-hit second run serves byte-identical exact ranges with no re-parse — existing second-run byte-identity assertions pass unmodified, plus an explicit cache-hit assertion that the served ranges are the exact ones (research D2: cache hits skip makeDocument)
    - MiniSwiftPackage integration assertions from 08-01 still green — compound-name `greet(name:)` base-identifier ends exact on defs and refs (RANGE-01)
  artifacts:
    - path: "Fixtures/UnicodeRangeFixture/Sources/UnicodeRange/main.swift"
      provides: "the exact research F4 source: `let emoji = \"🦖\"` / `let 名前 = greet(name: \"日本語\")` / blank / `func greet(name: String) -> String {` / `  \"Hello, \\(name) 🎉\"` plus a call site forcing the greet reference — byte-exact content, since the expectations are hand-computed against these bytes"
    - path: "Fixtures/UnicodeRangeFixture/Package.swift"
      provides: "minimal SwiftPM executable package (tools-version 6.2, one target) built with SwiftPMBuildRunner like MiniSwiftPackage"
    - path: "Tests/scip-swiftTests/IntegrationTests.swift"
      provides: "unicodeRangeFixture suite — full pipeline over the fixture, asserting the complete F4 range table plus the getter-drift proof, and a broken-source end-to-end test (RANGE-03)"
    - path: "Sources/scip-swift/SCIPMapping/SCIPIndexBuilder.swift"
      provides: "no behavior change beyond 08-01 wiring unless a gap surfaces — this plan's builder edits are limited to anything the integration tests prove missing (e.g. refiner construction path coverage); if no gap, the file is only re-verified"
  key_links:
    - UnicodeRangeFixture → SwiftPMBuildRunner.produceIndexStore() → SCIPIndexBuilder.build() → asserted Scip_Occurrence.singleLineRange values (the F4 table)
    - CacheStore content-hash hit → cached document with exact ranges → byte-identical second run (no re-parse of unchanged content)
    - Broken-source fixture file → Parser.parse error nodes → .present tokens for valid regions → approximation only where anchors miss
---

<objective>
Prove RANGE-01/02/03 end-to-end: the hand-computed F4 Unicode range table asserted against a real .scip built from a real fixture package, the getter-drift elimination demonstrated on the same fixture, broken-source indexing with graceful fallback through the full pipeline, and the cache-hit path serving exact ranges without re-parsing.

Purpose: Unit-level exactness (08-01) must hold through a real compiler build — IndexStoreDB anchors joined against real token maps, byte-for-byte.
Output: UnicodeRangeFixture + its integration suite + broken-source pipeline test + cache-hit exact-range assertion.
</objective>

<execution_context>
@$HOME/.codex/gsd-core/workflows/execute-plan.md
@$HOME/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-exact-occurrence-ranges/08-RESEARCH.md
@.planning/phases/08-exact-occurrence-ranges/08-VALIDATION.md
@.planning/phases/08-exact-occurrence-ranges/08-01-SUMMARY.md

@Sources/scip-swift/SCIPMapping/SCIPIndexBuilder.swift
@Sources/scip-swift/SourceRefinement/SwiftSyntaxRefiner.swift
@Tests/scip-swiftTests/IntegrationTests.swift
@Tests/scip-swiftTests/IncrementalIntegrationTests.swift
@Fixtures/MiniSwiftPackage/Package.swift
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: End-to-end "Unicode fixture emits the hand-computed F4 range table" — one path only</name>
  <files>Fixtures/UnicodeRangeFixture/Package.swift, Fixtures/UnicodeRangeFixture/Sources/UnicodeRange/main.swift, Tests/scip-swiftTests/IntegrationTests.swift</files>
  <precondition>08-01 landed: SwiftSyntaxRefiner wired into makeDocument and the MiniSwiftPackage tracer green.</precondition>
  <behavior>
    - fullPipeline over UnicodeRangeFixture produces one document with occurrences whose ranges match the F4 table exactly: emoji def L0 [4,9); getter:emoji L0 [4,9) — approximate would be 16; 名前 def L1 [4,10); getter:名前 L1 [4,10) — approximate would be 17; greet ref L1 [13,18); greet def L3 [5,10); name param def L3 [11,15); String refs L3 [17,23) and [28,35); init(stringInterpolation:) L4 [2,3); name ref L4 [12,16) (all 0-based, UTF-8 byte columns — research F4)
    - The getter:名前 assertion must fail if the refiner were removed (exact end 10 ≠ approximate end 17) — the drift proof is load-bearing, do not assert mere equality with current output
    - The fixture builds with SwiftPMBuildRunner exactly like MiniSwiftPackage tests (research D6)
  </behavior>
  <read_first>
    - 08-RESEARCH.md — §Key Verified Facts F4 (the hand-computed table and the exact source bytes), F3a/F3b (drift cases), Decisions D6
    - Tests/scip-swiftTests/IntegrationTests.swift — fullPipeline's runner/builder/document pattern and fixtureRepoPath helper to mirror for the new fixture
    - Fixtures/MiniSwiftPackage/Package.swift — the minimal package manifest shape to copy
    - Sources/scip-swift/SourceRefinement/SwiftSyntaxRefiner.swift — the API the assertions ride on (from 08-01)
  </read_first>
  <action>
    Create Fixtures/UnicodeRangeFixture with the F4 source byte-exact (research D4/F4): line 0 `let emoji = "🦖"`, line 1 `let 名前 = greet(name: "日本語")`, line 2 blank, line 3 `func greet(name: String) -> String {`, line 4 `  "Hello, \(name) 🎉"`. If top-level statements need a main entry for the package to build, add the minimal scaffolding the SwiftPM build requires (executable target with main.swift keeps top-level code legal) — but the five lines must occupy lines 0-4 with identical bytes, since the expectations are hand-computed against those bytes; any scaffolding goes after line 4 or the fixture uses a @main-free top-level-code layout like MiniSwiftPackage's shape. A `print` or discardable use may be needed so greet is actually referenced and the compiler emits the occurrence — the research probe observed the greet ref at L1 col 13, so the reference must sit on line 1 as written.

    RED first: add a @Test to IntegrationTests (new suite string "Integration: Unicode ranges" or extend the existing suite) running fullPipeline over the fixture and asserting the complete F4 table row by row — locate occurrences by symbol/display-name heuristics the way existing tests do (document.occurrences joined against document.symbols display names; getter occurrences surface via displayName `getter:emoji` / `getter:名前` — mirror how 08-01's tracer located getter:name). Include the two explicit anti-regression checks: getter:emoji end == 9 (not 16), getter:名前 end == 10 (not 17). Run the filter — it fails (fixture absent). Commit as test(08-02).

    GREEN: the fixture files themselves are the implementation — SwiftSyntaxRefiner wiring from 08-01 should make every row pass on first real run since the expectations were hexdump-verified. If a row fails, the defect is in the refiner or the fixture bytes drifted from F4 — fix the refiner or restore the exact bytes, never weaken an expectation. Run swift test --filter IntegrationTests green, commit as feat(08-02).
  </action>
  <verify>
    <automated>swift test --filter IntegrationTests</automated>
  </verify>
  <done>
    The Unicode fixture .scip matches the F4 table row for row; both getter drift proofs assert exact-≠-approximate; the fixture builds through the same SwiftPMBuildRunner path as MiniSwiftPackage.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Broken-source pipeline + cache-hit exact-range assertions</name>
  <files>Tests/scip-swiftTests/IntegrationTests.swift, Tests/scip-swiftTests/IncrementalIntegrationTests.swift, Sources/scip-swift/SCIPMapping/SCIPIndexBuilder.swift</files>
  <behavior>
    - A fixture file containing a syntax error still produces a document with occurrences through the full pipeline; occurrences anchored on valid tokens carry exact ends, any anchor that missed carries the approximate end — the document exists and is non-empty (RANGE-03 end-to-end, research F5)
    - Second run with a shared CacheStore serves byte-identical exact ranges — data1 == data2 as today, plus an explicit assertion that the cached run's occurrences carry the exact (not approximate) getter ends (research D2: cache hits skip makeDocument, cached docs already exact)
  </behavior>
  <read_first>
    - 08-RESEARCH.md — F5 (error recovery), D2/D4 (cache interaction: content-hash keyed, hits skip makeDocument)
    - Tests/scip-swiftTests/IncrementalIntegrationTests.swift — the two-builder shared-CacheStore pattern and secondRunIdentical assertions to extend, not duplicate
    - Tests/scip-swiftTests/IntegrationTests.swift — Task 1's unicode fixture helpers to reuse for the cache assertion
    - Sources/scip-swift/SCIPMapping/SCIPIndexBuilder.swift — the cache branch and makeDocument (verify the builder needs no edit; only edit if an assertion proves a gap)
  </read_first>
  <action>
    RED first. Broken-source test: extend the UnicodeRangeFixture with a second source file containing a genuine syntax error (e.g. a garbled declaration per research F5's `struct Broken { let x: = }` shape) alongside a valid function, so the package still compiles enough to index (SwiftPM compiles error files into the index store with error nodes — if the whole-module build fails hard on a non-recovering error, scope the error to a form the compiler recovers from per F5's verified .present-token behavior, and keep the valid symbol in the same file). Assert the broken file's document exists in the .scip and its valid-region occurrences carry exact ends; any miss falls back (assert at least one exact end present — the fallback path is already unit-proven in 08-01). Cache test: add an IncrementalIntegrationTests @Test that builds the Unicode fixture twice through one CacheStore and asserts byte-identity plus that the getter:名前 occurrence's end in run 2 is the exact 10.

    GREEN: builder edits only if an assertion exposes a real gap (e.g. the refiner must be constructed before the cache branch rather than inside makeDocument — it already is inside makeDocument, which cache hits skip, so expect no edit). Commit as feat(08-02) — or test(08-02) if tests alone went green with no source change, in which case drop Sources/scip-swift/SCIPMapping/SCIPIndexBuilder.swift from the commit.
  </action>
  <verify>
    <automated>swift test --filter IncrementalIntegrationTests && swift test --filter IntegrationTests && swift test --filter SwiftSyntaxRefinerTests && swift test --filter PositionMappingTests</automated>
  </verify>
  <done>
    Broken-source document indexes with exact ends on valid regions; second-run byte-identity holds with exact ranges served from cache; both unit suites stay green.
  </done>
</task>

<task type="auto">
  <name>Task 3: Wave gate — protected files + full phase partition</name>
  <files>.planning/phases/08-exact-occurrence-ranges/PHASE_BASE</files>
  <action>
    Protected-file gate against the PHASE_BASE commit captured in 08-01 Task 1: PositionMappingTests.swift (created in 08-01 with the approximation-fallback rows — must remain byte-identical through 08-02), SCIPSymbolFormatterTests.swift, IncrementalIntegrationTests.swift is modified this plan (cache-hit assertion added) so it is NOT protected here — the true untouched surface for Phase 8 is: Tests/scip-swiftTests/PositionMappingTests.swift (post-08-01 state), Tests/scip-swiftTests/SCIPSymbolFormatterTests.swift, Tests/scip-swiftTests/ScipIndexMergerTests.swift, Tests/scip-swiftTests/MultiRepoMergeIntegrationTests.swift, and Sources/scip-swift/SCIPMapping/PositionMapping.swift must differ only by the D3 signature addition — verify with git diff and confirm no behavioral change to the approximation branch (the PositionMappingTests rows are the executable proof). Revert any out-of-scope edit.
    Then run the full partition union green per 08-VALIDATION: swift test --skip Xcode plus the Xcode-filtered suites (the 300s local ceiling note applies — the cold swift-syntax compile is one-time).
  </action>
  <verify>
    <automated>swift test --skip Xcode && git diff --quiet "$(cat .planning/phases/08-exact-occurrence-ranges/PHASE_BASE)" -- Tests/scip-swiftTests/SCIPSymbolFormatterTests.swift Tests/scip-swiftTests/ScipIndexMergerTests.swift Tests/scip-swiftTests/MultiRepoMergeIntegrationTests.swift && git diff "$(cat .planning/phases/08-exact-occurrence-ranges/PHASE_BASE)" -- Sources/scip-swift/SCIPMapping/PositionMapping.swift | grep -c '^+' | grep -v '^0$'</automated>
  </verify>
  <done>
    Full non-xcodebuild partition green including the new Unicode/broken-source/cache suites; protected suites byte-identical to phase base; PositionMapping.swift diff contains only the D3 signature extension.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Fixture/broken source → pipeline | Error-containing repo source crosses the full build→index→refine path |
| Cache dir → process | Cached protobuf documents are re-served without re-parsing |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-08-05 | Tampering | Cache-store poisoning serving wrong ranges | medium | mitigate | Cache is content-hash keyed (research D2): a document's ranges can only be served for byte-identical source; second-run byte-identity assertions are the executable guard; manifest version invalidation from Phase 7 already bounds stale reads |
| T-08-06 | DoS | Broken-source fixture path failing the whole-module build | low | mitigate | Error scoped to a compiler-recovering form per F5 (verified .present tokens); fallback contract means worst case is approximate ends, never a dropped document |
| T-08-07 | Repudiation | None new — local CLI, no audit surface | low | accept | No auth/session/crypto/network change (research V2/V3/V4/V6: no) |
</threat_model>

<verification>
- swift test --filter IntegrationTests — the F4 table, getter drift proofs, and broken-source pipeline (run at task completion + wave end per 08-VALIDATION)
- swift test --filter IncrementalIntegrationTests — byte-identity plus cache-serves-exact assertion
- Protected-file gate against PHASE_BASE over the identity suites and merge suites; PositionMapping.swift diff limited to the D3 signature
- Phase gate: swift test --skip Xcode full partition green (08-VALIDATION full-suite row)
</verification>

<success_criteria>
- Unicode fixture .scip matches the complete hand-computed F4 range table (RANGE-02)
- getter:emoji / getter:名前 occurrences prove approximation drift eliminated end-to-end (RANGE-01)
- Broken-source file still indexes with exact ends on valid regions and graceful fallback elsewhere (RANGE-03)
- Cache-hit second run serves byte-identical exact ranges with no re-parse (research D2)
- All pre-phase suites and 08-01 suites green; protected files untouched
</success_criteria>

<output>
Create `.planning/phases/08-exact-occurrence-ranges/08-02-SUMMARY.md` when done
</output>
