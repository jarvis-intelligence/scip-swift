---
phase: 02-homebrew-distribution-release-pipeline
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - Sources/scip-swift/Build/BuildError.swift
  - Sources/scip-swift/IndexStore/IndexStoreLoader.swift
  - Tests/scip-swiftTests/DylibCheckTests.swift
autonomous: true
requirements:
  - DIST-04
estimate:
  tokens: 30000
  raw_tokens: 30000
  tasks: 1
  confidence: low
must_haves:
  truths:
    - "When libIndexStore.dylib is not found at the resolved toolchain path, scip-swift throws BuildError.xcodeRequired instead of crashing with an opaque IndexStoreLibrary error"
    - "The error message tells the user exactly what to do: install Xcode or run xcode-select --install"
    - "Existing functionality (successful dylib load on a machine with Xcode) is unaffected"
  artifacts:
    - path: "Sources/scip-swift/Build/BuildError.swift"
      provides: "New xcodeRequired(dylibPath:) case with actionable description"
      contains: "case xcodeRequired"
    - path: "Sources/scip-swift/IndexStore/IndexStoreLoader.swift"
      provides: "FileManager.default.fileExists guard before IndexStoreLibrary init"
      contains: "FileManager.default.fileExists"
    - path: "Tests/scip-swiftTests/DylibCheckTests.swift"
      provides: "Unit test verifying error message content"
      contains: "xcodeRequired"
  key_links:
    - from: "Sources/scip-swift/IndexStore/IndexStoreLoader.swift"
      to: "Sources/scip-swift/Build/BuildError.swift"
      via: "throws BuildError.xcodeRequired(dylibPath:) when FileManager check fails"
      pattern: "BuildError\\.xcodeRequired"
---

<objective>
Add a runtime guard that detects a missing libIndexStore.dylib and produces a clear, actionable error message instead of an opaque dyld/IndexStoreLibrary crash (DIST-04).

Purpose: Homebrew users who install scip-swift without Xcode (or without CommandLineTools) currently get a cryptic crash. This guard gives them a specific fix instruction: install Xcode or run xcode-select --install.

Output: Modified BuildError.swift (new case), modified IndexStoreLoader.swift (fileExists guard), new DylibCheckTests.swift (unit test).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-homebrew-distribution-release-pipeline/02-RESEARCH.md

@Sources/scip-swift/Build/BuildError.swift
@Sources/scip-swift/IndexStore/IndexStoreLoader.swift
@Sources/scip-swift/Platform/ToolchainInfo.swift
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Runtime dylib existence guard with actionable error (DIST-04)</name>
  <files>Sources/scip-swift/Build/BuildError.swift, Sources/scip-swift/IndexStore/IndexStoreLoader.swift, Tests/scip-swiftTests/DylibCheckTests.swift</files>
  <read_first>
    - Sources/scip-swift/Build/BuildError.swift — existing enum pattern (Error + CustomStringConvertible, exhaustive switch in description)
    - Sources/scip-swift/IndexStore/IndexStoreLoader.swift — current open(storePath:databasePath:) method (lines 6-12), exact insertion point
    - Sources/scip-swift/Platform/ToolchainInfo.swift — how dylibPath is resolved via xcrun --find swift (understand the path that the guard checks)
    - .planning/phases/02-homebrew-distribution-release-pipeline/02-RESEARCH.md "DIST-04: Runtime Dylib Check Code" section — exact code example and rationale
  </read_first>
  <behavior>
    - Test 1: BuildError.xcodeRequired(dylibPath: "/nonexistent/libIndexStore.dylib").description contains "libIndexStore.dylib"
    - Test 2: Same error description contains "Xcode" (so users know what to install)
    - Test 3: Same error description contains "xcode-select" (actionable command guidance)
    - Test 4: Existing IndexStoreLoader.open still compiles and existing tests pass (no regression on happy path)
  </behavior>
  <action>
    RED — Create Tests/scip-swiftTests/DylibCheckTests.swift as a Swift Testing @Suite("Dylib Check Error") with @Test functions verifying the behavior list above. The test constructs BuildError.xcodeRequired(dylibPath:) with a fake path and asserts the description string contains "libIndexStore.dylib", "Xcode", and "xcode-select". Run swift test --filter DylibCheckTests to confirm it fails (BuildError.xcodeRequired does not exist yet). Commit: test(02-01): add failing test for xcodeRequired error.

    GREEN — Implement in two files per the research code example (02-RESEARCH.md "DIST-04: Runtime Dylib Check Code"):

    1. In BuildError.swift: Add `case xcodeRequired(dylibPath: String)` to the enum. Add the matching case to the exhaustive `switch self` in the `description` property. The description must be a multi-line string that states the dylib path was not found, then gives three numbered steps: (a) install Xcode from the Mac App Store or run xcode-select --install for Command Line Tools, (b) if Xcode is installed but not selected, run sudo xcode-select -s /Applications/Xcode.app/Contents/Developer, (c) verify with xcrun --find swift. Follow the exact pattern of existing cases (structured context via associated value, actionable user-facing message). Reference: per DIST-04 and 02-RESEARCH.md Pitfall 5 (dylib may ship with CommandLineTools on recent macOS, so the message covers both scenarios).

    2. In IndexStoreLoader.swift: After the existing `let dylibPath = try ToolchainInfo.libIndexStoreDylibPath()` line and before the `let library = try IndexStoreLibrary(dylibPath: dylibPath)` line, add a `guard FileManager.default.fileExists(atPath: dylibPath) else { throw BuildError.xcodeRequired(dylibPath: dylibPath) }`. This is the earliest point where a missing dylib would crash — per the research rationale, checking here (not at startup) avoids false errors for --version/--help which never load the dylib.

    Run swift test --filter DylibCheckTests to confirm GREEN. Commit: feat(02-01): add runtime dylib existence guard.

    Do NOT refactor IndexStoreLoader.open to accept an injected dylib path — the guard is a simple fileExists check at the natural insertion point, no architectural change needed.
  </action>
  <verify>
    <automated>swift test --filter DylibCheckTests</automated>
  </verify>
  <done>
    BuildError.xcodeRequired(dylibPath:) exists with an actionable multi-step description containing "libIndexStore.dylib", "Xcode", and "xcode-select". IndexStoreLoader.open guards with FileManager.default.fileExists before IndexStoreLibrary init. DylibCheckTests pass. Full test suite still passes (swift test).
  </done>
  <reversibility rating="reversible">Adding an enum case and a guard statement — both can be removed without architectural impact.</reversibility>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Toolchain path → filesystem | dylibPath derived from xcrun output is checked via FileManager before loading |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-01 | Information Disclosure | BuildError.xcodeRequired description | low | accept | Error message includes the toolchain dylib path — this is a system path (e.g. /Applications/Xcode.app/...), not user-sensitive data. No mitigation needed. |
| T-02-02 | Denial of Service | IndexStoreLoader.open guard | low | accept | The fileExists check adds negligible latency (<1ms). No impact on normal operation. |
</threat_model>

<verification>
- swift test --filter DylibCheckTests passes (new test green)
- swift test passes (no regressions — all 54+ existing tests still green)
- swift build succeeds (new enum case compiles, guard compiles)
</verification>

<success_criteria>
- BuildError.xcodeRequired(dylibPath:) case exists with actionable description
- FileManager.default.fileExists guard is in IndexStoreLoader.open before IndexStoreLibrary init
- Unit test verifies error message content (dylib name, Xcode, xcode-select)
- No regressions in existing test suite
</success_criteria>

<output>
Create `.planning/phases/02-homebrew-distribution-release-pipeline/02-01-SUMMARY.md` when done
</output>
