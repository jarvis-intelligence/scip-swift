---
phase: 07-demangled-symbol-names
plan: 02
type: tdd
wave: 2
depends_on: ["07-01"]
files_modified:
  - Sources/scip-swift/SCIPMapping/SCIPIndexBuilder.swift
  - Sources/scip-swift/Commands/IndexCommand.swift
  - Tests/scip-swiftTests/IndexManifestTests.swift
  - Tests/scip-swiftTests/IntegrationTests.swift
  - Tests/scip-swiftTests/IncrementalIntegrationTests.swift
autonomous: true
requirements: [SYMBOL-03, SYMBOL-04]
estimate:
  tokens: 28000
  raw_tokens: 28000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - Running the binary with --no-demangle produces index output whose display_name values match v0.2.x opaque output exactly (SYMBOL-04)
    - Demangling stays default-on: no flag produces demangled display names (SYMBOL-04)
    - A second run over unchanged sources produces byte-identical output with demangling on — existing IncrementalIntegrationTests assertions pass unmodified (SYMBOL-03)
    - Cross-repo merge dedup is unchanged — ScipIndexMergerTests and MultiRepoMergeIntegrationTests pass without modification (SYMBOL-03)
    - A cache dir written by the 0.2.1 converter regenerates (not serves) documents after the 0.3.0 upgrade — the cache-upgrade integration test proves it (SYMBOL-03, research D4/Pitfall 4)
    - External symbols carry demangled display names when demangling is on and stay empty otherwise, matching v0.2.x (research D7)
  artifacts:
    - path: "Sources/scip-swift/Commands/IndexCommand.swift"
      provides: "@Flag(noDemangle) parsed and threaded as demangle: !noDemangle through indexOneRepo into SCIPIndexBuilder(demangle:)"
    - path: "Sources/scip-swift/SCIPMapping/SCIPIndexBuilder.swift"
      provides: "externalSymbols entries with displayName filled from the demangler when demangling is on"
    - path: "Tests/scip-swiftTests/IndexManifestTests.swift"
      provides: "converterVersion mismatch test asserting a 0.2.1-written manifest is incompatible with the 0.3.0 constant"
    - path: "Tests/scip-swiftTests/IntegrationTests.swift"
      provides: "--no-demangle v0.2.x parity test built on the demangle: false seam"
  key_links:
    - IndexCommand @Flag noDemangle -> indexOneRepo(demangle:) defaulted param -> SCIPIndexBuilder(demangle:) -> demangler nil-out
    - ScipSwiftVersion 0.3.0 (from 07-01) -> manifest isCompatibleWith comparison -> CacheStore.invalidateAll -> regenerated documents
---

<objective>
Finish the phase: thread the --no-demangle flag CLI → indexOneRepo → builder (SYMBOL-04, research D5), fill external-symbol display names (research D7), and prove the identity/caching contract end-to-end with a cache-upgrade test (SYMBOL-03, research D4). Expansion on the slice 07-01 proved.

Purpose: Users must be able to reproduce v0.2.x output byte-for-byte, and mixed old/new caches must never serve stale short names after the version bump.
Output: Flag threading, external display names, cache-upgrade + parity tests, full-suite green with merge/incremental suites untouched.
</objective>

<execution_context>
@$HOME/.codex/gsd-core/workflows/execute-plan.md
@$HOME/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-demangled-symbol-names/07-RESEARCH.md
@.planning/phases/07-demangled-symbol-names/07-VALIDATION.md
@.planning/phases/07-demangled-symbol-names/07-01-SUMMARY.md

@Sources/scip-swift/SCIPMapping/SCIPIndexBuilder.swift
@Sources/scip-swift/Commands/IndexCommand.swift
@Sources/scip-swift/Commands/IndexManyCommand.swift
@Tests/scip-swiftTests/IndexManifestTests.swift
@Tests/scip-swiftTests/IncrementalIntegrationTests.swift
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: --no-demangle CLI flag threaded to the builder (SYMBOL-04)</name>
  <files>
    Sources/scip-swift/Commands/IndexCommand.swift,
    Tests/scip-swiftTests/IntegrationTests.swift
  </files>
  <read_first>
    - 07-RESEARCH.md — Decision D5 (flag help text, defaulted-param threading, IndexManyCommand deliberately gets no flag) and Decision D6
    - Sources/scip-swift/Commands/IndexCommand.swift — the @Flag block (indexOnly at lines 32-33), run()'s call site, indexOneRepo's signature with the Phase-6 destination: String? = nil pattern, and the builder construction site
    - Sources/scip-swift/Commands/IndexManyCommand.swift — the indexOneRepo call site that must compile unchanged
    - Tests/scip-swiftTests/IntegrationTests.swift — the 07-01 re-baselined fullPipeline and the temporary-directory helpers
  </read_first>
  <behavior>
    - Integration test "demangle off reproduces v0.2.x opaque display names": a builder constructed with demangle: false over the fixture produces display-name values identical to the pre-phase short names (Greeter, greet(), name) — snapshot them as exact-match assertions, matching today's lines-43-46 expectations the phase replaced
    - Integration test "external symbols stay empty when demangle is off": index.externalSymbols' displayName values are all empty (research D7 v0.2.x parity)
    - The flag parses: binary --help output contains --no-demangle (cheap CLI-surface check)
  </behavior>
  <action>
    RED first: add the two builder-level parity tests to IntegrationTests (the demangle: false parameter exists from 07-01; these assertions on externals are the RED rows). Run the filter — the externals row fails (07-01 leaves them empty, so it may pass trivially; if so the RED evidence is the flag-wiring compile state) — commit test(07-02).

    GREEN: add @Flag(name: .long, help: "Emit v0.2.x opaque symbol display names instead of demangled ones.") var noDemangle = false to IndexCommand beside indexOnly. Thread it as demangle: Bool = true on indexOneRepo (after symbolVersion), passing demangle: !noDemangle from run(), and forward into SCIPIndexBuilder(demangle:) at the construction site. Default the parameter so IndexManyCommand compiles unchanged — make no edits there (research D5: no flag on index-many, Phase-6 precedent). Commit feat(07-02).
  </action>
  <verify>
    <automated>swift test --filter IntegrationTests</automated>
  </verify>
  <done>
    --no-demangle parses and reaches the builder as demangle: false; demangle-off output matches v0.2.x display names exactly; externals empty; demangling default-on; IndexManyCommand untouched and compiling.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: external-symbol display names (research D7)</name>
  <files>
    Sources/scip-swift/SCIPMapping/SCIPIndexBuilder.swift
  </files>
  <read_first>
    - 07-RESEARCH.md — Decision D7 (externals gain demangled names, e.g. Swift.String.count.getter : Swift.Int) and D6 (memoized instance consulted in the externalSymbols loop)
    - Sources/scip-swift/SCIPMapping/SCIPIndexBuilder.swift — the externalSymbols loop (build(), info.symbol = sym, currently no displayName) and the per-run demangler from 07-01
    - Tests/scip-swiftTests/IntegrationTests.swift — the Task 1 externals test that asserts the off-mode empty behavior
  </read_first>
  <behavior>
    - Demangle on: fixture externals include at least one demangled name containing "Swift.String" (e.g. the verified Swift.String.count.getter : Swift.Int shape); no external displayName is an empty string when its USR demangles
    - Demangle off: every external displayName stays empty (Task 1's test keeps guarding this)
    - externalSymbols array stays sorted by canonical symbol string — ordering changes nothing
  </behavior>
  <action>
    Extend the Task 1 externals test with the on-mode assertions (RED), then in build()'s externalSymbols loop consult the same per-run demangler: set info.displayName = demangler?.demangledDisplayName(usr: <the USR for that canonical string>) ?? "" — the USR is recoverable the way makeDocument had it while iterating occurrences; since the loop sees only the canonical string, keep a per-run [String: String] canonical-string → USR map accumulated in makeDocument for non-local symbols (or pass the demangled name through referencedSymbols) — choose the smallest change consistent with D6's single-memoized-instance rule and note the choice in the summary. Off-mode leaves displayName empty via the nil demangler. Keep the sort untouched. Commit feat(07-02).
  </action>
  <verify>
    <automated>swift test --filter IntegrationTests</automated>
  </verify>
  <done>
    Externals carry demangled display names when on; empty when off; canonical external symbol strings and ordering unchanged; no second demangler instance created per run.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: cache-upgrade invalidation + identity regression gate (SYMBOL-03)</name>
  <files>
    Tests/scip-swiftTests/IndexManifestTests.swift,
    Tests/scip-swiftTests/IncrementalIntegrationTests.swift
  </files>
  <read_first>
    - 07-RESEARCH.md — Decision D4 (manifest mechanism), Pitfall 4 (stale-cache warning sign), Pitfall 5 (merge regression guard)
    - Tests/scip-swiftTests/IndexManifestTests.swift — the existing converterVersion-differs test at line 47 to mirror
    - Tests/scip-swiftTests/IncrementalIntegrationTests.swift — secondRunIdentical and cacheMissThenHit as the pattern for the new upgrade test
    - Sources/scip-swift/Commands/IndexCommand.swift — the manifest isCompatibleWith / invalidateAll branch the upgrade test drives through indexOneRepo
  </read_first>
  <behavior>
    - Unit: IndexManifest written with converterVersion "0.2.1" reports isCompatibleWith(converterVersion: ScipSwiftVersion.version) == false (mirror of the existing test at IndexManifestTests.swift:47)
    - Integration "0.2.1 cache dir regenerates after upgrade": run indexOneRepo over the fixture with a hand-seeded cache — write a CacheStore manifest carrying converterVersion "0.2.1" plus at least one stale serialized Scip_Document under docs/ whose displayName is a v0.2.x short name for a source file that still hashes identical — then run indexOneRepo with the real ScipSwiftVersion.version; the produced document for that file carries the demangled display name (proves regeneration, not serving)
    - The stale-seeding must not hardcode internal cache filenames beyond <hash>.scipdoc; derive the hash via ContentHasher the same way the builder does
  </behavior>
  <action>
    RED first: add both tests. Run their filters — the unit row may already pass (mechanism predates this phase; that is acceptable evidence, keep it as a regression row), the integration row fails or passes depending on whether 07-01's version bump already wired invalidation — the value here is the end-to-end proof that a poisoned cache cannot serve stale short names. Commit test(07-02).

    Then the phase gate: swift test --skip Xcode (per 07-VALIDATION.md wave boundary) — IncrementalIntegrationTests and both merge suites must pass WITHOUT modification (Pitfall 5: ScipIndexMergerTests and MultiRepoMergeIntegrationTests are the untouched regression guards; secondRunIdentical now exercises the demangled path via the default). If any guard fails, the defect is in this phase's display-only discipline — fix the production change, never the guard test. Commit with the test additions.
  </action>
  <verify>
    <automated>swift test --filter IndexManifestTests && swift test --filter IncrementalIntegrationTests && swift test --filter ScipIndexMerger</automated>
  </verify>
  <done>
    Cache-upgrade integration test green (stale 0.2.1 cache regenerates demangled documents); manifest mismatch row green; existing IncrementalIntegrationTests assertions pass unmodified alongside the new cache-upgrade test in that file; ScipIndexMergerTests, MultiRepoMergeIntegrationTests green with zero edits to the merge suites.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CLI user → flag parsing | --no-demangle is a Boolean flag; no free-text value crosses this boundary |
| Disk cache → index output | Documents cached by an older converter version are re-served into fresh output unless invalidation fires |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-07-04 | Tampering | stale cache documents served post-upgrade | medium | mitigate | ScipSwiftVersion 0.3.0 (07-01) keyed into IndexManifest.converterVersion drives invalidateAll before any load; the Task 3 upgrade test proves a poisoned cache regenerates demangled documents |
| T-07-05 | Repudiation | --no-demangle output provenance | low | accept | metadata.toolInfo.arguments already records the CLI argv including the flag, so any produced index self-documents whether demangling ran |
</threat_model>

<verification>
- swift test --filter IntegrationTests — parity, externals, and end-to-end gate
- swift test --filter IndexManifestTests && IncrementalIntegrationTests && ScipIndexMerger — SYMBOL-03 contract gates
- swift test --skip Xcode — wave-boundary gate per 07-VALIDATION.md (merge + incremental suites untouched)
- Phase gate before verify-work: swift test (full partition union)
</verification>

<success_criteria>
- --no-demangle reproduces v0.2.x display-name output exactly; demangling is default-on (SYMBOL-04)
- Second-run byte-identity and cross-repo merge dedup green with zero modification to those suites (SYMBOL-03)
- Cache-upgrade test proves 0.2.1 caches regenerate after the bump (research D4)
- External symbols demangle when on, empty when off (research D7)
</success_criteria>

<output>
Create `.planning/phases/07-demangled-symbol-names/07-02-SUMMARY.md` when done
</output>
