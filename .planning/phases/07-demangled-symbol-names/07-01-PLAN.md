---
phase: 07-demangled-symbol-names
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - Sources/scip-swift/SCIPMapping/USRDemangler.swift
  - Sources/scip-swift/Platform/ToolchainInfo.swift
  - Sources/scip-swift/SCIPMapping/SCIPIndexBuilder.swift
  - Sources/scip-swift/Version.swift
  - Tests/scip-swiftTests/IntegrationTests.swift
  - Tests/scip-swiftTests/USRDemanglerTests.swift
autonomous: true
requirements: [SYMBOL-01, SYMBOL-02]
estimate:
  tokens: 30000
  raw_tokens: 30000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - The fixture .scip carries "MiniSwiftPackage.Greeter.greet() -> Swift.String" as a display name while that symbol's canonical `symbol` string still embeds the raw USR s:16MiniSwiftPackage7GreeterV5greetSSyF verbatim (SYMBOL-01 + identity untouched)
    - c:-prefixed, closure, local-suffix, and garbage USRs never throw and never yield an empty display name — the caller keeps the IndexStoreDB short name (SYMBOL-02)
    - Locals (local <n> symbols) keep their short names (research D3)
    - A dlopen or xcrun resolution failure degrades to always-fallback; indexing still completes (research A1 fail-soft)
    - ScipSwiftVersion reads 0.3.0 in the same commit that changes display names, so stale cached documents invalidate (research D4, Pitfall 4)
  artifacts:
    - path: "Sources/scip-swift/SCIPMapping/USRDemangler.swift"
      provides: "final class USRDemangler — failable init(dylibPath:), static load() toolchain factory, demangledDisplayName(usr:) -> String? with s:-gate, memoization, n+1 truncation retry, nil fallback"
    - path: "Sources/scip-swift/Platform/ToolchainInfo.swift"
      provides: "libswiftDemangleDylibPath() mirroring the existing libIndexStoreDylibPath() xcrun resolution"
    - path: "Sources/scip-swift/SCIPMapping/SCIPIndexBuilder.swift"
      provides: "demangle: Bool = true init parameter (omittable at all existing call sites); displayName set from the demangler for non-local symbols only, at the current displayName assignment site"
    - path: "Sources/scip-swift/Version.swift"
      provides: "version constant 0.3.0"
    - path: "Tests/scip-swiftTests/USRDemanglerTests.swift"
      provides: "verified USR corpus + fallback-contract unit suite"
  key_links:
    - ToolchainInfo.libswiftDemangleDylibPath() -> USRDemangler.load() -> SCIPIndexBuilder.build() per-run demangler -> makeDocument displayName assignment
    - Version 0.3.0 -> IndexManifest converterVersion comparison in indexOneRepo -> CacheStore.invalidateAll() on mismatch
---

<objective>
Build the USR demangler (dlopen of the toolchain's libswiftDemangle.dylib, research D2) and wire ONE path end-to-end: a real fixture USR demangled into a .scip's SymbolInformation.display_name with symbol identity untouched, plus the fallback contract as a unit corpus (SYMBOL-01, SYMBOL-02). Display-only design per research D1 — demangled text lands in display_name; the canonical symbol string, its formatter, and all identity-bearing paths stay untouched. The cache-version bump lands in the same task set as the display change per research D4/Pitfall 4.

Purpose: Raw `s:`-prefixed USRs are unreadable in SCIP consumers; the display_name field exists in the vendored proto for exactly this purpose.
Output: USRDemangler module + toolchain dylib resolution + minimal builder wiring + version bump + unit corpus.
</objective>

<execution_context>
@$HOME/.codex/gsd-core/workflows/execute-plan.md
@$HOME/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-demangled-symbol-names/07-RESEARCH.md
@.planning/phases/07-demangled-symbol-names/07-VALIDATION.md

@Sources/scip-swift/SCIPMapping/SCIPIndexBuilder.swift
@Sources/scip-swift/Platform/ToolchainInfo.swift
@Sources/scip-swift/SCIPMapping/SCIPSymbolFormatter.swift
@Sources/scip-swift/Version.swift
@Tests/scip-swiftTests/IntegrationTests.swift
</context>

<tasks>

<task type="tracer">
  <name>End-to-end "one real USR demangled into a .scip display_name" — one path only</name>
  <files>
    Sources/scip-swift/SCIPMapping/USRDemangler.swift,
    Sources/scip-swift/Platform/ToolchainInfo.swift,
    Sources/scip-swift/SCIPMapping/SCIPIndexBuilder.swift,
    Sources/scip-swift/Version.swift,
    Tests/scip-swiftTests/IntegrationTests.swift
  </files>
  <precondition>xcrun --find swift resolves to the pinned 6.2.4 toolchain and the sibling lib/libswiftDemangle.dylib exists (research A1 environment check — if absent, the fail-soft contract still ships but the tracer's demangled assertions cannot go green; halt and report instead of weakening them).</precondition>
  <behavior>
    - fullPipeline over the MiniSwiftPackage fixture produces display names containing "MiniSwiftPackage.Greeter" and "MiniSwiftPackage.Greeter.greet() -> Swift.String" (both strings verified empirically in research §Key Verified Facts)
    - The canonical `symbol` string for greet() still embeds the raw USR s:16MiniSwiftPackage7GreeterV5greetSSyF verbatim, backtick-wrapped exactly as SCIPSymbolFormatter renders it today
    - Local symbols' display names are unchanged short names
  </behavior>
  <read_first>
    - 07-RESEARCH.md — §Code Examples "Demangler core" skeleton, §Key Verified Facts corpus, Decisions D1–D4, Pitfalls 1–4
    - Sources/scip-swift/Platform/ToolchainInfo.swift — the xcrun --find swift → toolchain-root resolution to mirror
    - Sources/scip-swift/SCIPMapping/SCIPIndexBuilder.swift — makeDocument's displayName assignment (currently the short name), build()'s ownership of per-run state, the inout-accumulator parameters
    - Sources/scip-swift/SCIPMapping/SCIPSymbolFormatter.swift — the canonical string shape the identity assertions must preserve (do not edit this file)
    - Tests/scip-swiftTests/IntegrationTests.swift — fullPipeline and the displayNames assertion block at lines 43-46
  </read_first>
  <action>
    RED first: extend fullPipeline with the two demangled display-name expectations from <behavior> plus one identity expectation — the document's greet symbol string embeds the raw s:16MiniSwiftPackage7GreeterV5greetSSyF USR verbatim (match the exact escaping the formatter's tests expect; read SCIPSymbolFormatterTests for the wrapped form). Run the filter — only the new expectations fail. Commit as test(07-01).

    GREEN, five edits in one commit:
    1. ToolchainInfo: add libswiftDemangleDylibPath() mirroring libIndexStoreDylibPath() line for line — same xcrun --find swift invocation, same toolchain-root derivation, resolving to lib/libswiftDemangle.dylib; same BuildError style on failure.
    2. New final class USRDemangler in Sources/scip-swift/SCIPMapping/USRDemangler.swift (research D2/D6 — a class, not an enum namespace, because it memoizes; the stateless-mapper convention is untouched). Failable init(dylibPath:) performs dlopen(path, RTLD_LAZY) then dlsym of "swift_demangle_getDemangledName", bit-cast to the @convention(c) typealias (UnsafePointer<CChar>?, UnsafeMutablePointer<CChar>?, UInt) -> UInt. Static load() factory calls ToolchainInfo.libswiftDemangleDylibPath() and returns nil on ANY failure — thrown error, nil dlopen handle, nil dlsym — never propagating an error (fail-soft per research A1 and SYMBOL-02). demangledDisplayName(usr:) -> String?: return nil unless usr.hasPrefix("s:") (research Pitfall 2 — never rewrite c:/so:/_: USRs); consult the memo dict; rewrite input to "_$s" + usr.dropFirst(2); call the C function with a caller-owned [CChar] buffer starting at 64 bytes so ordinary corpus outputs force the truncation-retry path; returned n == 0 means failure → nil; n >= buffer capacity means truncated → retry once with capacity n+1 (verified contract: truncation returns the full required length); an empty decoded string → nil. The memo dict must preserve the nil contract — a failed or empty USR may not later surface as an empty-string display name. WHY comments are warranted and expected only at the dlopen-resolution site and the caller-owned-buffer/no-free semantics (project comment policy allows non-obvious WHY).
    3. SCIPIndexBuilder: add init parameter demangle: Bool = true (defaulted so every existing call site — IndexCommand, IntegrationTests, IncrementalIntegrationTests, MultiRepoMerge helper — compiles unchanged, the Phase-6 destination pattern). In build(), create the per-run instance: let demangler = demangle ? USRDemangler.load() : nil, and pass it into makeDocument. In makeDocument, change only the displayName assignment: non-local symbols get demangler?.demangledDisplayName(usr: symbol.usr) ?? short name; local symbols keep the short name (research D3). Nothing else in the builder changes this plan — external-symbol display names are 07-02 scope.
    4. Version.swift: change the version constant from 0.2.1 to 0.3.0 (research D4). Same commit as the display change so a stale cache can never serve short names after upgrade (Pitfall 4).
    5. Re-baseline IntegrationTests lines 43-46: replace the three short-name exact-match expectations with the demangled expectations from the RED step; keep the raw-USR identity expectation as a permanent guard.
    Run the IntegrationTests filter green, then commit as feat(07-01).

    Do not touch the canonical formatter source or its 11-test suite, the incremental byte-identity suite, or either merge suite — they are this phase's untouched regression guards (research Pitfall 5).
  </action>
  <verify>
    <automated>swift test --filter IntegrationTests && test -z "$(git status --porcelain -- Sources/scip-swift/SCIPMapping/SCIPSymbolFormatter.swift Tests/scip-swiftTests/SCIPSymbolFormatterTests.swift Tests/scip-swiftTests/IncrementalIntegrationTests.swift Tests/scip-swiftTests/ScipIndexMergerTests.swift Tests/scip-swiftTests/MultiRepoMergeIntegrationTests.swift)"</automated>
  </verify>
  <done>
    Fixture .scip shows demangled display names for non-local Swift symbols; locals keep short names; greet's canonical symbol string still embeds the raw USR verbatim; version constant is 0.3.0; the formatter, its tests, the incremental suite, and both merge suites are byte-for-byte unmodified; suite green.
  </done>
</task>

<task type="auto" tdd="true">
  <name>USRDemanglerTests unit corpus — SYMBOL-01 strings + SYMBOL-02 fallback contract</name>
  <files>
    Tests/scip-swiftTests/USRDemanglerTests.swift
  </files>
  <behavior>
    - Corpus (research §Key Verified Facts, all empirically verified): s:16MiniSwiftPackage7GreeterV → "MiniSwiftPackage.Greeter"; s:16MiniSwiftPackage7GreeterV5greetSSyF → "MiniSwiftPackage.Greeter.greet() -> Swift.String"; s:16MiniSwiftPackage7GreeterV4nameACSS_tcfc → "MiniSwiftPackage.Greeter.init(name: Swift.String) -> MiniSwiftPackage.Greeter"; s:16MiniSwiftPackage7GreeterV4nameSSvg → "MiniSwiftPackage.Greeter.name.getter : Swift.String"; s:SS → "Swift.String"; s:SS5countSivg → "Swift.String.count.getter : Swift.Int"
    - Fallback returns nil (never throws, never empty): c:objc(cs)NSObject; c:@F@printf; s:-prefixed closure s:4null5greetyyFyS2ScfU_; s:-prefixed local s:4null5outer5inneryyFyyxlF; s:garbage; empty string
    - Truncation: the init USR (longest corpus output, 60+ characters) returns its full exact string — the 64-byte initial buffer guarantees the n+1 retry path runs
    - Fail-soft: USRDemangler(dylibPath: "/nonexistent/libswiftDemangle.dylib") yields a nil instance
    - Determinism: repeated calls for the same USR return identical results
  </behavior>
  <read_first>
    - 07-RESEARCH.md — §Key Verified Facts and §Validation Architecture Wave-0 rows for SYMBOL-01/SYMBOL-02
    - Tests/scip-swiftTests/SCIPSymbolFormatterTests.swift — the repo's @Suite/@Test string-description style to match
    - Sources/scip-swift/SCIPMapping/USRDemangler.swift — the API surface from Task 1
  </read_first>
  <action>
    Add @Suite("USRDemanglerTests") with one @Test per behavior row above, string descriptions in the repo style. Construct instances via USRDemangler.load() for the corpus/fallback/determinism rows (real toolchain, sub-second, no fixture build) and via the explicit-path failable init for the missing-dylib row. Run the filter — corpus rows should pass against the tracer implementation since every expectation was empirically verified against this toolchain; any failure is a genuine defect in the rewrite, buffer, or memoization logic of the Task 1 module — fix the module, never weaken an assertion. Commit as test(07-01).
  </action>
  <verify>
    <automated>swift test --filter USRDemanglerTests</automated>
  </verify>
  <done>
    Suite green in under a few seconds with no fixture build; all six corpus pairs exact-match; every fallback row returns nil; the nil-instance fail-soft row passes; no assertion weakened to get green.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Toolchain → process | dlopen loads the toolchain's own libswiftDemangle.dylib; the path is derived from xcrun output, never from user input |
| Index store → C ABI | Compiler-produced USR strings cross into a C function with a caller-owned fixed-size buffer |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-07-01 | Tampering | dlopen target resolution | medium | mitigate | Path comes only from ToolchainInfo's xcrun --find swift derivation (research D2); no CLI option, env var, or index-store value ever reaches the dlopen path; RTLD_LOCAL handle scoped to the process |
| T-07-02 | DoS | C ABI buffer handling | low | mitigate | Caller-owned [CChar] buffer with a bounded single n+1 retry; every failure mode returns 0 → nil fallback (verified), so no unbounded growth and no crash path from malformed USRs |
| T-07-03 | Information Disclosure | demangled names in index output | low | accept | Demangled names reveal module/type names already present verbatim inside the raw USRs emitted today; no new data leaves the repo |
</threat_model>

<verification>
- swift test --filter USRDemanglerTests — fast pure-unit gate for corpus + fallback (run after every task commit per 07-VALIDATION.md)
- swift test --filter IntegrationTests — end-to-end tracer gate with re-baselined display expectations
- Protected-file gate: git status --porcelain over the formatter source/tests, IncrementalIntegrationTests, ScipIndexMergerTests, MultiRepoMergeIntegrationTests stays empty
</verification>

<success_criteria>
- Real fixture USRs demangle to the verified human-readable strings and land in display_name (SYMBOL-01)
- Undemanglable inputs (c:, closures, locals-suffix, garbage) and a missing dylib all fall back to the short name; nothing ever throws or emits an empty display name (SYMBOL-02)
- Canonical symbol strings, the formatter, incremental byte-identity, and merge dedup are untouched
- Version 0.3.0 lands with the display change so caches invalidate (research D4)
</success_criteria>

<output>
Create `.planning/phases/07-demangled-symbol-names/07-01-SUMMARY.md` when done
</output>
